# Claims Registry

| ID | Claim | Source | Page | Confidence | Status | Notes |
|----|-------|--------|------|------------|--------|-------|
| C-0001 | Symphony's core loop is poll Plane → select candidate issue → render prompt → dispatch to agent → comment results back. | `wiki/raw/symphony-context.md#8` | `wiki/concepts/symphony-engine.md` | high | active | First ingest. |
| C-0002 | A Project Binding maps exactly one Plane project to one git repo plus its dispatch config. | `wiki/raw/symphony-context.md#11-12` | `wiki/concepts/symphony-engine.md` | high | active | First ingest. |
| C-0003 | All Bindings share one workspace-scoped Plane API key for the single `homelab` workspace, differing only by `plane_project_id`. | `wiki/raw/symphony-context.md#12` | `wiki/concepts/symphony-engine.md` | high | active | First ingest. |
| C-0004 | Mode values are plan, build, execute (default); execute is the absence of plan and build labels. | `wiki/raw/symphony-context.md#16,32` | `wiki/concepts/symphony-engine.md` | high | active | First ingest. Superseded for Podium by C-0060 — ADR-0005 drops Mode entirely; Skill replaces it as the work-shape lever. |
| C-0005 | A Binding whose repo has no readable `WORKFLOW.md` is a hard config error — Symphony refuses to dispatch and posts a blocked comment. | `wiki/raw/symphony-context.md#24` | `wiki/concepts/symphony-engine.md` | high | active | First ingest. |
| C-0006 | The Verdict vocabulary is done / review / blocked, declared by the agent via `SYMPHONY_RESULT:`; last occurrence wins. | `wiki/raw/symphony-context.md#44` | `wiki/concepts/symphony-engine.md` | high | active | First ingest. |
| C-0007 | Landing defaults to local: Run Worktree torn down, branch ref kept committed and unpushed for manual rpiv-merge. No auto-push or auto-merge by default. | `wiki/raw/symphony-context.md#56` | `wiki/concepts/symphony-engine.md` | high | active | First ingest. Thin engine v2 (e73e924) removes worktrees and landing logic for coding bindings — agent owns git operations. |
| C-0008 | The Done Marker is a per-run nonce the agent prints when finished, used to detect completion of tmux sessions that have no process exit code. | `wiki/raw/symphony-context.md#40` | `wiki/concepts/symphony-engine.md` | high | active | First ingest. |
| C-0009 | Each Run executes in its own Run Worktree (isolated git worktree + branch); a global cap bounds how many Runs are live at once. | `wiki/raw/symphony-context.md#52,68` | `wiki/concepts/symphony-engine.md` | high | active | First ingest. Superseded for coding bindings by thin engine v2 (e73e924) — worktrees removed, agent runs in repo checkout. |
| C-0010 | The approval-gate policy is opt-in, default off; the homelab Binding opts in. | `wiki/raw/symphony-context.md#12` | `wiki/concepts/symphony-engine.md` | high | active | First ingest. Note: bindings.yml shows `approval.enabled: false` for homelab; flag is the engine gate, label-driven plan/approve flow is separate. |
| C-0011 | Symphony supports one-shot ticket scheduling only; recurring patrol work belongs in Temporal schedules on aidev. | `wiki/raw/runbook-symphony.md#99-103` | `wiki/concepts/symphony-operations.md` | high | active | Batch ingest. |
| C-0012 | Label-only scheduled tickets default to the next 12am-6am America/Los_Angeles maintenance window when no Symphony-Schedule comment exists. | `wiki/raw/runbook-symphony.md#122` | `wiki/concepts/symphony-operations.md` | high | active | Batch ingest. |
| C-0013 | Blocked reconciler defaults: ENABLED=true, APPLY=false, INTERVAL_MS=1800000 (30 min); first tick after service start runs scan immediately. | `wiki/raw/runbook-symphony.md#147-158` | `wiki/concepts/symphony-operations.md` | high | active | Batch ingest. |
| C-0014 | Blocked reconciler counts distinct `Patrol pass for ...` comments newer than the latest failure comment; does not trust consecutive_passes=N. Tickets with approval-required are skipped. | `wiki/raw/runbook-symphony.md#160-166` | `wiki/concepts/symphony-operations.md` | high | active | Batch ingest. |
| C-0015 | Telegram notifications fire on IN_REVIEW and BLOCKED transitions only; scheduled and released ticket notifications are intentionally disabled. Failures log warnings, never block scheduler. | `wiki/raw/runbook-symphony.md#199-208` | `wiki/concepts/symphony-operations.md` | high | active | Batch ingest. |
| C-0016 | Claude dispatch uses tmux send-keys (private socket, load-buffer/paste-buffer + Enter) because Anthropic removed usable `claude -p` for this account. | `wiki/raw/adr-0001-claude-tmux.md#3-5` | `wiki/analyses/adr-0001-claude-tmux.md` | high | active | Batch ingest. Historical — ClaudeAgentAdapter deleted in thin engine v2 (e73e924); pi-only for v2. |
| C-0017 | Symphony rejected adopting off-the-shelf orchestrators (sortie, Composio, Warren/overstory, Code Conductor, GitHub Agent HQ) because every mature platform assumes headless agent dispatch. | `wiki/raw/adr-0002-generalize-symphony.md#3-7` | `wiki/analyses/adr-0002-generalize-symphony.md` | high | active | Batch ingest. |
| C-0018 | Worktree-per-run replaces (does not layer on) the old single global flock; per-tick flock is replaced by a live-run semaphore. | `wiki/raw/adr-0003-worktree-per-run.md#5,7` | `wiki/analyses/adr-0003-worktree-per-run.md` | high | active | Batch ingest. Superseded for coding bindings by thin engine v2 (e73e924) — no worktrees. |
| C-0019 | Symphony keeps no database; cap count and live-Run set are in-memory and lost on service restart, so startup must reconcile from durable signals (git worktree entries, per-run tmux sessions, Plane Running issues). Requires deterministic run-id → worktree path / branch / tmux session naming. | `wiki/raw/adr-0003-worktree-per-run.md#7` | `wiki/analyses/adr-0003-worktree-per-run.md` | high | active | Batch ingest. Thin engine v2 removes worktrees and tmux dispatch for coding bindings — reconcile relies solely on Plane Running issues. |
| C-0020 | Plan→build artifact handoff under worktree-per-run uses a git-ref handoff: plan-handoff comment records plan Run's branch ref; build Run creates worktree off that plan branch. Reuses `_PLAN_HANDOFF_MARKER` and `_plan_path_from_comments`, changing only the payload. | `wiki/raw/adr-0003-worktree-per-run.md#13` | `wiki/analyses/adr-0003-worktree-per-run.md` | high | active | Batch ingest. Superseded for coding bindings by thin engine v2 — plan→build handoff removed for coding binding_type. |
| C-0021 | TrackerRole required label Roles are MODE_PLAN and MODE_BUILD; required state Roles are all five states; APPROVAL_REQUIRED, APPROVED, SCHEDULED, HAS_WORKTREE are optional and absence disables behaviour. | `wiki/raw/tracker_contract.py#28-38,174` | `wiki/concepts/tracker-contract.md` | high | active | Batch ingest. |
| C-0022 | Engine vocabulary moved from `homelab_router.plane_contract` into Symphony-owned `tracker_contract.py`; homelab is now just another Binding (per ADR-0004). | `wiki/raw/adr-0004-tracker-contract.md#3-5` | `wiki/analyses/adr-0004-tracker-contract.md` | high | active | Batch ingest. |
| C-0023 | trading Binding omits the SCHEDULED and APPROVED Roles, so the scheduled-release and plan-approval paths are inactive for that project. | `wiki/raw/bindings.yml` | `wiki/entities/binding-trading.md` | high | superseded | Batch ingest. Superseded by C-0107: #023d removed trading's entire `tracker_contract` block (2026-06-11), so trading now resolves to `DEFAULT_CONTRACT` rather than a leaner hand-tuned Role set. The cited `wiki/raw/bindings.yml` snapshot predates the removal. |
| C-0024 | symphony-pi-executor-swap dropped the brainstorm's `--no-context-files` lock so pi can load `AGENTS.md`/`CLAUDE.md` from the homelab repo; WORKFLOW.md line 27 depends on AGENTS.md safety rules. | `wiki/raw/plan-pi-executor-swap.md` | `wiki/analyses/symphony-plan-history.md` | high | active | Batch ingest. |
| C-0025 | pi can exit 0 with empty stdout/stderr on auth or model misconfiguration; scheduler would otherwise treat as a successful no-op — explicit guardrails added in the pi-executor-swap plan. | `wiki/raw/plan-pi-executor-swap.md` | `wiki/analyses/symphony-plan-history.md` | high | active | Batch ingest. |
| C-0026 | homelab WORKFLOW.md enables medium-risk autonomy by default for routine execute tickets and approved build tickets; allowed mutations must have a verification command that proves recovery within 2-5 minutes. | `wiki/raw/workflow-homelab.md#30-52` | `wiki/entities/workflow-homelab.md` | high | active | Tier 1. |
| C-0027 | Symphony itself, Jellyfin, TrueNAS, Proxmox are schedule-only services for homelab — work on them without schedule context or James approval is blocked. | `wiki/raw/workflow-homelab.md#58-64` | `wiki/entities/workflow-homelab.md` | high | active | Tier 1. |
| C-0028 | Reboots are allowed only when the ticket is scheduled for the current maintenance window; otherwise schedule or block. | `wiki/raw/workflow-homelab.md#65-72` | `wiki/entities/workflow-homelab.md` | high | active | Tier 1. |
| C-0029 | trading WORKFLOW.md requires explicit approval **in the Plane issue body** naming the exact live action before any live trading, exchange API, real order, real portfolio, production Temporal namespace, or long-running agent action; otherwise sandbox-only work. | `wiki/raw/workflow-trading.md#23-26` | `wiki/entities/workflow-trading.md` | high | active | Tier 1. |
| C-0030 | trading WORKFLOW.md plan-artifact path is `Plans/<identifier>.md` (capital P, identifier not slug) — different convention from homelab's `plans/<slug>.md`. | `wiki/raw/workflow-trading.md#17-18` | `wiki/entities/workflow-trading.md` | high | active | Tier 1. |
| C-0031 | symphony-host.service unit retains `OPENCODE_BIN` and `SYMPHONY_OPENCODE_AGENT=build` Environment= entries despite the pi-swap landing; zero references in current .py source. Safe to leave; safe to remove at a future unit cleanup. | `wiki/raw/symphony-host.service#16,18` | `wiki/sources/symphony-host-service-unit.md` | high | active | Tier 1. |
| C-0032 | Pi-swap brainstorm originally locked `--no-context-files` but Codex review caught that WORKFLOW.md line 27 depends on homelab AGENTS.md; James reversed the lock and the landed plan drops `--no-context-files`. The brainstorm row is historically superseded. | `wiki/raw/brainstorm-pi-swap.md#75`, `wiki/raw/plan-pi-executor-swap.md` | `wiki/analyses/brainstorm-pi-swap.md` | high | active | Tier 1. |
| C-0033 | Symphony-Schedule comment grammar accepts only `key="quoted value with spaces"` or `key=bare-value-without-spaces`; missing whitespace between tokens raises ScheduleParseError (no silent split). | `schedule.py#114-124,487-496` | `wiki/concepts/schedule-comment-grammar.md` | high | active | Tier 2. |
| C-0034 | latest_event() isolates control-plane candidates BEFORE sorting so unrelated chatter cannot perturb the winner (round-4 audit finding 3). | `schedule.py#746-766` | `wiki/concepts/schedule-comment-grammar.md` | high | active | Tier 2. |
| C-0035 | Blocked-reconciler counts DISTINCT pass comments since latest failure because the homelab patrol worker upserts a fresh ticket per cycle, resetting consecutive_passes=1 on every run (real AUTO-110 evidence). | `blocked_reconciler.py#82-92` | `wiki/concepts/blocked-reconciler-implementation.md` | high | active | Tier 2. |
| C-0036 | Default blocked-reconciler rule list ships ONLY the `patrol-passes` rule active; a `symphony-completed-review` fallback exists but has a non-matching prefix so it cannot fire by accident — operator must edit the file. | `blocked_reconciler.py#112-126` | `wiki/concepts/blocked-reconciler-implementation.md` | high | active | Tier 2. |
| C-0037 | scheduler.py uses a per-Binding _DispatchState with its own asyncio.Semaphore replacing the legacy single global flock; module-level _RUN_SEMAPHORE is retained only for backward compat with tests calling run_tick / _dispatch_one directly. | `scheduler.py#48-57,94-115` | `wiki/concepts/scheduler-loop.md` | high | active | Tier 2. |
| C-0038 | scheduler.py rate-limit cooldown on Plane 429 uses base=30s, max=300s, ±20% jitter, per-Binding state. | `scheduler.py#74-76,117-190` | `wiki/concepts/scheduler-loop.md` | high | active | Tier 2. Superseded/expanded by C-0051: cooldown now also has host-level `_PLANE_COOLDOWN_UNTIL`. |
| C-0039 | scheduler.py CLAIM_PREFIX is `"Symphony claimed at "` and appends `code_sha=<sha>` after the timestamp; backwards-compatible because _claimed_at splits on CLAIM_PREFIX and takes the first whitespace token. | `scheduler.py#58-65` | `wiki/concepts/scheduler-loop.md` | high | active | Tier 2. |
| C-0040 | run_worktree run_id is `sha256(identifier.strip().lower())[:8]`; deterministic so durable signals (worktree dirs, branches, tmux sessions) match back to a Run without storing an in-memory map (per ADR-0003 C-0019). | `run_worktree.py#24-58` | `wiki/concepts/agent-runner-and-worktree.md` | high | active | Tier 2. run_worktree.py deleted in thin engine v2 (e73e924). Historical only. |
| C-0041 | run_worktree naming: `worktrees_root/run-<id>` (path), `symphony/run-<id>` (branch), `symphony-<id>` (tmux session), `symphony-run-<id>` (tmux socket). | `run_worktree.py#36-58` | `wiki/concepts/agent-runner-and-worktree.md` | high | active | Tier 2. run_worktree.py deleted in thin engine v2. Historical only. |
| C-0042 | remove_worktree keeps the branch ref (contains the Run's committed changes for later landing); only the worktree directory is removed. | `run_worktree.py#315-356` | `wiki/concepts/agent-runner-and-worktree.md` | high | active | Tier 2. run_worktree.py deleted in thin engine v2. Historical only. |
| C-0043 | verify_pi_support is two-step: (1) pi --help advertises --print and --no-session; (2) one-token probe `pi --print --no-session --provider <p> --model <m> ping` from cwd=<repo>; fail if rc!=0 or stdout.strip()=="" (counters silent-failure). | `agent_runner.py#85-145` | `wiki/concepts/agent-runner-and-worktree.md` | high | active | Tier 2. |
| C-0044 | ClaudeAgentAdapter Done Marker is `SYMPHONY_DONE_<nonce>` where nonce is `uuid.uuid4().hex`; prompt epilogue tells the agent to print it on its own line when complete. | `agent_runner.py#287-308,383-388` | `wiki/concepts/agent-runner-and-worktree.md` | high | active | Tier 2. ClaudeAgentAdapter deleted in thin engine v2 (e73e924). Historical only. |
| C-0045 | prompt_renderer adds a runtime `conversation` Mode (default `IssueData.mode = "conversation"`) not present in CONTEXT.md's plan/build/execute Mode list — engine-side mode resolution maps labels to plan/build/execute, but the renderer respects whatever Mode the scheduler hands it. | `prompt_renderer.py#27,141-157` | `wiki/concepts/prompt-renderer.md` | medium | active | Tier 2. Flag for CONTEXT.md update. |
| C-0046 | prompt_renderer truncates the previous-comments block at 12000 chars tail-first, prepending "[Earlier previous comments truncated]" so recent context survives. | `prompt_renderer.py#17,103-118` | `wiki/concepts/prompt-renderer.md` | high | active | Tier 2. |
| C-0047 | prompt_renderer untrusted-block escape replaces `</issue>` with `< /issue>` (and `</previous_comments>` similarly) to defend against issue content escaping the wrapping block. | `prompt_renderer.py#67-75` | `wiki/concepts/prompt-renderer.md` | high | active | Tier 2. |
| C-0048 | tests/ holds 14 test files with 435 tests total; test_scheduler.py is the coverage hotspot at 160 tests. Validation contract: `python3 -m pytest -q` + `py_compile *.py` + `git diff --check`. | `tests/*.py`, `wiki/raw/runbook-symphony.md#128-134` | `wiki/analyses/symphony-tests-index.md` | high | active | Tier 3. |
| C-0049 | All 8 symphony-* skills enforce safety gates: typed-slug confirmation for archive/scaffold, James approval for restart/Plane writes, refusal on stub WORKFLOW.md for smoke. The umbrella symphony-onboard-project does not bypass any sub-skill's gate. | 8 SKILL.md files | `wiki/analyses/symphony-skills-index.md` | high | active | Tier 3. Plane-era skill-suite picture superseded for migrated Podium paths by C-0099..C-0102; still useful for historical/global skill copies and restart gates. |

| C-0050 | Post-agent PlaneRateLimitError after clean agent exit now records pending review state and retries/reclaims Running issues with existing worktrees instead of waiting only for stale timeout. | `scheduler.py#93-114,919-930,1495-1670`, `tests/test_scheduler.py#4315-4375`, `wiki/raw/sessions/2026-06-09-trading-smoke-rate-limit.md` | `wiki/analyses/trading-smoke-rate-limit-debugging.md` | high | active | Session update; explains `a269e32`. |
| C-0051 | Plane 429 cooldown is now shared across binding dispatch states via `_PLANE_COOLDOWN_UNTIL`, while retaining per-binding cooldown state. | `scheduler.py#57,117-173`, `tests/test_scheduler.py#3964-3990`, `wiki/raw/sessions/2026-06-09-trading-smoke-rate-limit.md` | `wiki/concepts/scheduler-loop.md` | high | active | Supersedes the per-binding-only wording in C-0038. |
| C-0052 | Optional `has-worktree` label without a configured UUID no longer triggers Plane label-resolution scans; absence disables label behavior rather than paginating labels. | `scheduler.py#850-855`, `tests/test_scheduler.py#529-557`, `wiki/raw/sessions/2026-06-09-trading-smoke-rate-limit.md` | `wiki/analyses/trading-smoke-rate-limit-debugging.md` | high | active | Session update; explains `c4944be`. |
| C-0053 | Unlabeled tickets render as `conversation` mode, whose injected context forbids file edits; dirty-worktree landing proof must use plan/build or future explicit execute support. | `prompt_renderer.py#141-157`, `wiki/raw/sessions/2026-06-09-trading-smoke-rate-limit.md` | `wiki/concepts/prompt-renderer.md` | high | active | Reinforces C-0045 with live smoke evidence. |
| C-0054 | Podium `POST /api/bindings/{name}/issues` returns 201 with server-set `state='todo'`; `state` in the body is 400, invalid values 422, unknown binding 404 with binding-existence checked before body validation. `reasoning_effort`/`worktree_active`/`base_branch` are server-defaulted (high / false / bindings.yml) but client-settable. | `web/api/main.py`, `web/api/tests/test_issue_create.py` | `wiki/analyses/podium-014-new-issue-flow.md` | high | active | Session update; commits `a68cccf`, `a6157f3`. |
| C-0055 | Podium skill seeding is per-row `INSERT OR IGNORE` over `SEED_SKILLS` (now incl. `/diagnose`) at every boot — it resurrects deleted seed rows and never rewrites descriptions, so #015's catalog refresh must retire `_seed_skills` or own the table. | `web/api/seed.py`, `.kanban/issues/015-podium-skill-catalog.md` | `wiki/analyses/podium-014-new-issue-flow.md` | high | superseded | Session update; commit `a68cccf`. Superseded by C-0136: `_seed_skills`/`SEED_SKILLS` were removed from `web/api/seed.py`; the refresh CLI now owns the `skill` table and removed seed rows stay removed across restarts. |
| C-0056 | Podium `GET /api/bindings/{name}/options` serves dropdown choices: agents statically mirror the scheduler's `_validate_agent` set (`pi`, `claude`; `config.py:471`), models are the curated `KNOWN_MODELS` placeholder (no catalog source of truth exists), branches come live from `git for-each-ref` against the binding's `repo_path` and degrade to `[]` on failure. | `web/api/main.py`, `wiki/raw/sessions/2026-06-10-podium-014-new-issue-flow.md` | `wiki/analyses/podium-014-new-issue-flow.md` | high | superseded | Session update; commit `bf7cfd0`. Superseded by C-0114: #028 removed `KNOWN_MODELS` and made `models.yml` the model catalog source of truth. |
| C-0057 | Podium new-issue modal prepends an optimistic temp card but closes only on POST success; failure rolls the card back and keeps the modal open with typed values and an error line. Flyout dropped the priority and max-duration chips (PATCH support retained) per James. | `web/frontend/components/NewIssueModal.tsx`, `web/frontend/tests/new-issue.spec.ts`, `wiki/raw/sessions/2026-06-10-podium-014-new-issue-flow.md` | `wiki/analyses/podium-014-new-issue-flow.md` | high | active | Session update; commits `f0de67b`, `4aab377`. Deviation from #014 spec's "close on submit" accepted by James. |
| C-0058 | `issue.preferred_agent` and `preferred_model` are free text end to end (only `preferred_skill` is FK-validated); #020 dispatch must fall back to binding defaults on unknown values. | `web/api/main.py`, `.kanban/issues/020-podium-trading-cutover.md` | `wiki/analyses/podium-014-new-issue-flow.md` | high | active | Session update. |
| C-0059 | ADR-0005 retires Plane and builds Podium, a Symphony-native tracker + operator console: FastAPI:8090 + Next.js App Router:8091, SQLite at `/var/lib/symphony/podium.db`, Alembic migrations. Plane archived after both bindings cut over (trading first, then homelab). | `wiki/raw/adr-0005-replace-plane-with-podium.md#5,11`, `web/api/db.py` | `wiki/analyses/adr-0005-replace-plane-with-podium.md` | high | active | ADR-0005 ingest. Archive sequence in progress: trading archived 2026-06-11 (#023d, C-0107); homelab archive deferred to a follow-up issue. |
| C-0060 | Podium drops Mode entirely; **Skill** replaces it as the work-shape lever, structured as a CLI-refreshed `skill` catalog table + per-Issue `preferred_skill` + per-Run `skill_invoked`. | `wiki/raw/adr-0005-replace-plane-with-podium.md#5,7`, `web/api/schema.py:14-18,29,62` | `wiki/analyses/adr-0005-replace-plane-with-podium.md` | high | active | ADR-0005 ingest. Supersedes C-0004 for Podium. |
| C-0061 | In Podium the Binding **is** the Project — there is no separate Project entity; the Plane Project layer existed only because Plane could not address bindings directly. | `wiki/raw/adr-0005-replace-plane-with-podium.md#5,7`, `web/api/schema.py:6-12` | `wiki/analyses/adr-0005-replace-plane-with-podium.md` | high | active | ADR-0005 ingest. |
| C-0062 | Run becomes a first-class `run` table (one row per dispatch, mutable state machine, log → `runs/{id}.log`, no event-log table v1). On Symphony startup any row still `queued`/`running` is reaped to a synthetic `failed`/`blocked` verdict with a `restart-orphan` summary. Issue carries a latest-Run projection (`latest_run_id/_verdict/_run_state`, `last_event_at`) so the board reads without joins. | `wiki/raw/adr-0005-replace-plane-with-podium.md#5`, `web/api/schema.py:38-65` | `wiki/analyses/adr-0005-replace-plane-with-podium.md` | high | active | ADR-0005 ingest. Replaces ADR-0003 in-memory reconcile (C-0019) now that Podium has a DB. Run-log location refined by C-0082. |
| C-0063 | `worktree_active` is opt-in per-Issue (default false → agent runs in the repo checkout, thin-engine-v2 behavior; true → persistent per-Issue branch+worktree). On Issue→Done with true, Podium fast-forward-merges the branch to base then tears down; conflict/diverged/force-pushed base aborts the merge, leaves the worktree, posts a blocked comment — no merge-commit, no force-push, no rebase. This reintroduces worktrees as operator-controlled, reversing ADR-0003's engine-default posture. | `wiki/raw/adr-0005-replace-plane-with-podium.md#5,9`, `web/api/schema.py:31` | `wiki/analyses/adr-0005-replace-plane-with-podium.md` | high | active | ADR-0005 ingest. Posture shift vs C-0009/C-0018. |
| C-0064 | `bindings.yml` gains a `tracker: plane\|podium` field; both impls satisfy ADR-0004's role contract by different mechanisms — Plane resolves Roles to label/state names + UUIDs, Podium projects Roles directly onto typed columns (`mode:*` + approval/scheduled fold into columns or disappear, `agent:*` → `preferred_agent`, five states stay as enum). ADR-0004's role-as-engine-interface stands; its Plane-shaped contract is partially superseded for Podium. | `wiki/raw/adr-0005-replace-plane-with-podium.md#5,9` | `wiki/analyses/adr-0005-replace-plane-with-podium.md` | high | active | ADR-0005 ingest. Partially supersedes C-0021. |
| C-0065 | Podium adds two sibling systemd units — `podium-api.service`, `podium-web.service` — each with its own `OnFailure=telegram-alert@%n.service`; `symphony-host.service` is unchanged. Both ports bind localhost; external access via Authelia reverse proxy on 9091. (Updated 2026-06-12 #023d: the web frontend now binds `10.20.20.16:8091` for the `podium.testytech.net` proxy — C-0109; the API still binds loopback.) Auth = single bcrypt-hashed shared password `PODIUM_PASSWORD_HASH` from `/home/james/symphony-host.env` (single-user; multi-user explicit non-goal). | `wiki/raw/adr-0005-replace-plane-with-podium.md#5,11` | `wiki/analyses/adr-0005-replace-plane-with-podium.md` | high | active | ADR-0005 ingest. Units landed/verified by C-0103. |
| C-0066 | Known bug: `scheduler.py:488` keys `is_coding` off `config.bindings[0].binding_type` alone, so the whole `is_coding` branch resolves against whichever binding is first in `bindings.yml` rather than per-issue. Flagged in ADR-0005 for cleanup, out of ADR scope. | `scheduler.py:488`, `wiki/raw/adr-0005-replace-plane-with-podium.md#3` | `wiki/analyses/adr-0005-replace-plane-with-podium.md` | high | active | ADR-0005 ingest. Verified live at scheduler.py:488. |
| C-0067 | Podium has two distinct state enums: `issue.state` ∈ {todo,in_review,running,blocked,done,archived} (board lifecycle) vs `run.state` ∈ {queued,running,succeeded,failed} (dispatch machine, also `issue.latest_run_state`); verdict ∈ {done,review,blocked}. DB path resolves `PODIUM_DB_PATH` → `/var/lib/symphony/podium.db` (if parent writable) → repo-root `podium.db`. | `web/api/schema.py`, `web/api/db.py:8-22`, `.kanban/issues/034-podium-archived-state-core.md` | `wiki/concepts/podium-tracker.md` | high | active | ADR-0005 ingest; updated by #034 to include `archived`. Note (C-0082): `RUN_LOG_ROOT` does NOT mirror this fallback — run logs co-locate with the resolved DB via `adapter.db_path`, not `RUN_LOG_ROOT`. |
| C-0068 | Podium keeps `comments_md` (bidirectional human↔AI thread: humans write fully, AI writes concise summaries) and `context_md` (AI-only session log: AI writes full output, AI reads on dispatch) as separate Issue blobs. When `context_md` exceeds threshold, Symphony invokes the configured agent with a hardcoded compaction prompt — engine-built compaction, not a Skill, zero schema impact. | `wiki/raw/adr-0005-replace-plane-with-podium.md#5`, `web/api/schema.py:34-35` | `wiki/analyses/adr-0005-replace-plane-with-podium.md` | high | superseded | ADR-0005 design-era claim. Superseded by C-0095: implementation added `binding_settings` schema for threshold/keep values, so "zero schema impact" is no longer true. |
| C-0069 | Podium exposes `WS /api/ws` and uses an in-process `WebSocketHub` with one bounded asyncio queue per subscriber; no Redis, broker, room filtering, or multi-worker fanout exists. | `web/api/main.py` | `wiki/analyses/podium-017-live-updates.md` | high | active | #017 session update; requires uvicorn single-worker deployment. |
| C-0070 | Podium issue mutations publish live events after successful writes: PATCH publishes `issue.updated`, POST publishes `issue.created`, and startup seed publishes placeholder `run.updated` for newly seeded Run rows. | `web/api/main.py`, `web/api/seed.py`, `web/api/tests/test_websocket.py` | `wiki/analyses/podium-017-live-updates.md` | high | active | #020 must add real engine Run mutation events. **Update (C-0112, 2026-06-12): this never happened and architecturally cannot via the in-process hub** — the scheduler is a separate process; ADR-0006 resolves engine-state freshness with gated polling instead. |
| C-0071 | Podium frontend opens one WebSocket per browser session, applies messages to TanStack Query caches, invalidates visible binding/issue queries on reconnect, and renders `Disconnected — retrying` while disconnected. | `web/frontend/components/QueryProvider.tsx`, `web/frontend/app/layout.tsx`, `web/frontend/tests/live-sync.spec.ts` | `wiki/analyses/podium-017-live-updates.md` | high | active | #017 session update. |
| C-0072 | Podium live tabs keep last-write-wins Issue editing semantics; #017 did not add version columns, ETags, or conditional PATCH. | `web/api/main.py`, `wiki/analyses/podium-017-live-updates.md` | `wiki/analyses/podium-017-live-updates.md` | high | active | Decision from #017 carry-over review. |
| C-0073 | Podium #018 startup requires both `PODIUM_PASSWORD_HASH` and `PODIUM_SESSION_SECRET`; `web/api/auth.py` loads `.env` read-only for dev and raises a clear runtime error when either env var is missing. | `web/api/auth.py`, `web/api/main.py`, `web/api/tests/test_auth.py` | `wiki/analyses/podium-018-auth.md` | high | active | Session update; commit `b8a50f0`. |
| C-0074 | Podium #018 protects all HTTP `/api/*` routes except `/api/auth/*` and `/api/health`, and also requires a valid `podium_session` cookie before accepting `WS /api/ws`. | `web/api/main.py`, `web/api/tests/test_auth.py`, `web/api/tests/test_websocket.py` | `wiki/analyses/podium-018-auth.md` | high | active | Extends C-0069/C-0071 with auth requirement. |
| C-0075 | Podium login uses bcrypt verification, signed httpOnly `podium_session` cookies with `SameSite=Lax` and `Max-Age=86400`; failed passwords sleep 250ms, and five failures per IP per minute trigger 429 with `Retry-After: 60`. | `web/api/auth.py`, `web/api/main.py`, `web/api/tests/test_auth.py` | `wiki/analyses/podium-018-auth.md` | high | active | Session update; commit `b8a50f0`. |
| C-0076 | Podium frontend routes now check `/api/auth/whoami` and redirect unauthenticated users to `/login`; the CLI helper `python -m web.cli.podium set-password` prints a bcrypt env assignment to stdout only and writes no secret file. | `web/frontend/components/AppShell.tsx`, `web/frontend/app/login/page.tsx`, `web/frontend/tests/auth.spec.ts`, `web/cli/podium.py`, `web/cli/tests/test_skills_refresh.py` | `wiki/analyses/podium-018-auth.md` | high | active | Session update; e2e tests authenticate with `page.request.post('/api/auth/login')`. |
| C-0077 | `prompt_renderer.render_prompt(...)` now accepts `tracker_kind: "plane"|"podium"` defaulting to `plane`; the Podium path reads `comments_md` without 12K truncation, renders `context_md` in an Issue Context block, and leaves Plane behavior unchanged by default. | `prompt_renderer.py`, `tests/test_prompt_renderer_podium.py` | `wiki/concepts/prompt-renderer.md` | high | active | Session update for #025; implementation commit `36a7cd4`. |
| C-0078 | `skill_mode_map.py` is the transitional single source for Podium Skill→legacy Mode projection: `/dev-plan`→`plan`, `/dev-build`→`build`, `/diagnose` and `/code-review`→`execute`, unknown or missing skill→`execute`. | `skill_mode_map.py`, `tests/test_prompt_renderer_podium.py` | `wiki/concepts/prompt-renderer.md` | high | active | Bridges Podium `preferred_skill` to existing `{{issue.mode}}` templates until Mode retirement completes. |
| C-0079 | `config.py` now validates per-binding `tracker: plane|podium`; omitted values default to `plane`, and unknown values raise `ConfigError`. | `config.py`, `tests/test_config.py` | `wiki/concepts/podium-tracker.md` | high | active | #019 implementation. |
| C-0080 | `tracker_podium.py` implements the runtime `TrackerAdapter` surface against SQLite without a direct `plane_adapter` import; coding-binding Roles project to `issue.state`, `preferred_skill`, and `preferred_agent`, while approval/scheduled infra projection remains deferred to #023c. | `tracker_podium.py`, `tracker_adapter.py`, `tests/test_tracker_podium.py` | `wiki/concepts/podium-tracker.md` | high | superseded | #019 implementation; direct Plane import absence verified by grep in review. Superseded by C-0105: #023c implemented infra approval/schedule columns and projection. |
| C-0081 | Scheduler success handling writes full sanitized stdout/stderr into Podium `context_md` only for adapters with `stores_context=True`, while keeping Plane behavior unchanged. | `scheduler.py`, `tracker_podium.py`, `tests/test_engine_against_podium.py` | `wiki/concepts/podium-tracker.md` | high | active | #019 implementation. |
| C-0082 | Podium run logs are written to `<active podium.db parent>/runs/<id>.log` (e.g. `/home/james/symphony/runs/<id>.log` in fallback mode), NOT the `/var/lib/symphony/runs` `RUN_LOG_ROOT` default — that default applies only once a writable `/var/lib/symphony` exists. The default is reached when `adapter.db_path` is `None`, which crashed the live #020 cutover: `main._build_binding_runtime` builds `PodiumTrackerAdapter` without `db_path`, so `_start_run_record` fell back to the unwritable root and `_write_run_log`'s `mkdir` raised `PermissionError`, leaving runs unfinalized (issue reached In Review only via the stale-running reconciler). Fixed by resolving `db_path` in `__post_init__` (`8eb4aa6`). | `tracker_podium.py`, `scheduler.py:438-439`, `web/api/db.py:8-22`, `tests/test_trading_podium_dispatch.py` | `wiki/analyses/analysis-session-020-cutover-smoke.md` | high | active | #020 cutover smoke. Refines C-0062 and C-0067 (`RUN_LOG_ROOT` did not mirror `resolve_db_path`'s fallback). Regression test `test_trading_podium_dispatch_logs_colocate_with_resolved_db` builds the adapter as `main` does and fails without the fix. |
| C-0083 | The `trading` binding went live on Podium on 2026-06-11 (#020 cutover). The smoke (issue 17 → run 6, filed by direct `podium.db` insert because the Podium web UI/API was not running) reached `state=succeeded`/`verdict=review` in ~59s with log on disk and `comments_md`/`context_md` populated, via the normal completion path. cost/token columns stay null when the agent emits no `SYMPHONY_COST_USD`/`*_TOKENS` markers. | `bindings.yml:63-65`, `.kanban/issues/020-podium-trading-cutover.md`, `wiki/raw/sessions/2026-06-11-podium-020-cutover-smoke.md` | `wiki/analyses/analysis-session-020-cutover-smoke.md` | high | active | #020 cutover smoke. Original note said homelab remained on Plane; superseded by C-0104. |
| C-0084 | Podium #021 implements `worktree_active=true` dispatch by creating/reusing `worktrees/<binding>/<issue_id>` on branch `podium/<binding>/<issue_id>` and running `pi` with that worktree as subprocess `cwd`; false preserves thin-engine v2 repo-checkout dispatch. | `web/api/worktree.py`, `agent_runner.py`, `tests/test_agent_runner.py` | `wiki/analyses/podium-021-worktree-auto-merge.md` | high | active | Refines C-0063 with implementation evidence. |
| C-0085 | `CandidateIssue` now carries `worktree_active`, `base_branch`, and `binding_name`; `PodiumTrackerAdapter.list_candidates()` populates them from the SQLite `issue` row so the agent runner can select the correct worktree branch/base. | `plane_adapter.py`, `tracker_podium.py`, `agent_runner.py` | `wiki/analyses/podium-021-worktree-auto-merge.md` | high | active | Plane candidate fields default inert for existing Plane path. |
| C-0086 | Podium Issue `state -> done` with `worktree_active=true` resolves the binding repo, checks dirty base state, checks out `base_branch`, runs `git merge --ff-only podium/<binding>/<issue_id>` off the FastAPI event loop via `asyncio.to_thread`, and on success removes both worktree and branch ref. | `web/api/main.py`, `web/api/worktree.py`, `web/api/tests/test_worktree_api.py` | `wiki/analyses/podium-021-worktree-auto-merge.md` | high | active | Implements ADR-0005's FF-only merge policy. |
| C-0087 | Podium auto-merge aborts move the issue to `blocked`, append an operator-facing `comments_md` message, publish the final blocked row over WebSocket, and leave the worktree intact. Covered aborts include dirty base checkout, conflict/diverged base, force-pushed base, and unknown repo path. | `web/api/main.py`, `web/api/tests/test_worktree_api.py` | `wiki/analyses/podium-021-worktree-auto-merge.md` | high | active | No rebase, force-push, or merge-commit fallback. |
| C-0088 | Toggling `worktree_active` from true to false while a worktree exists appends a "Worktree archived" comment and does not delete the worktree; the Issue API projects server-derived `worktree_path` / `worktree_branch` fields, and the frontend chip renders those fields while active and clears once the issue is Done. | `web/api/main.py`, `web/frontend/lib/api.ts`, `web/frontend/components/IssueFlyout.tsx`, `web/frontend/tests/worktree.spec.ts` | `wiki/analyses/podium-021-worktree-auto-merge.md` | high | active | Operator intent preserved; UI e2e covers chip lifecycle. |
| C-0089 | Podium startup reconciliation now reaps durable queued/running Run rows before legacy stale-running Issue reconciliation; the hook is optional and no-ops for adapters without `reconcile_orphaned_runs`. | `scheduler.py#1332-1367`, `scheduler.py#1420-1435` | `wiki/analyses/podium-022-run-reconcile-log-retention.md` | high | active | Implements ADR-0005 startup reaper in code; refines C-0062 from design to landed behavior. |
| C-0090 | `PodiumTrackerAdapter.reconcile_orphaned_runs()` sets queued/running rows to `failed`/`blocked` with `restart-orphan` summary, refreshes the Issue latest-Run projection, marks the parent Issue `blocked`, appends an orphan comment, and preserves worktrees. | `tracker_podium.py#355-415`, `tests/test_run_reconcile.py#102-148` | `wiki/analyses/podium-022-run-reconcile-log-retention.md` | high | active | Parent Issue state becomes `blocked`; worktree directories remain for operator inspection. |
| C-0091 | Podium run-log retention deletes only log files older than 90 days or beyond the newest 100 logs per Issue, keeps Run DB rows, and clears `run.log_path` to NULL for reaped logs; it runs once at startup and then every 24h from `run_loop`. | `scheduler.py#74`, `scheduler.py#1434-1435`, `scheduler.py#1547-1566`, `tracker_podium.py#417-472`, `tests/test_log_retention.py#100-196` | `wiki/analyses/podium-022-run-reconcile-log-retention.md` | high | active | Structured log pairs are `log_retention_begin/done`; run reconciliation pairs are `run_reconcile_begin/done`. |
| C-0092 | Podium Alembic baseline verification upgrades a fresh SQLite DB to head, fingerprints tables/columns/foreign keys/indexes against runtime `SCHEMA_SQL`, and asserts migration history remains a single linear chain rooted at `0001_initial`. | `tests/test_alembic_baseline.py`, `web/api/migrations/README.md` | `wiki/analyses/podium-023b-alembic-backup.md` | high | active | #023b implementation. Alembic `fileConfig(..., disable_existing_loggers=False)` preserves pytest log capture after migrations. |
| C-0093 | Podium host backup uses cron because `rsnapshot` is absent: `/etc/cron.d/podium-backup` runs `scripts/podium-backup.sh` daily as `james`; the script resolves the active DB/log paths, writes `/backup/podium-YYYY-MM-DD.db`, optionally archives runs, and rotates artifacts after 14 days. | `scripts/podium-backup.sh`, `wiki/raw/podium-backup.cron`, `web/README.md` | `wiki/analyses/podium-023b-alembic-backup.md` | high | active | Restore drill created `/backup/podium-2026-06-11.db` and matched schema + row counts against the active DB. |
| C-0094 | Symphony dev test tooling is pinned to pytest 8.x and pytest-asyncio 0.x because pytest 9 / pytest-asyncio 1 changed logging behavior enough to break existing `caplog` assertions after Alembic ran. | `pyproject.toml`, `uv.lock`, `wiki/raw/sessions/2026-06-11-podium-023b-alembic-backup.md` | `wiki/analyses/podium-023b-alembic-backup.md` | high | active | Revisit when tests are updated for pytest 9 logging behavior. |
| C-0095 | Podium #026 implements Issue Context compaction with a new `binding_settings` table containing `context_compact_threshold_tokens` (default 16000) and `context_compact_keep_recent_runs` (default 3), via Alembic revision `0002_context_compaction_settings`. | `web/api/schema.py`, `web/api/migrations/versions/0002_context_compaction_settings.py`, `tests/test_alembic_baseline.py` | `wiki/analyses/podium-026-context-compaction.md` | high | active | Supersedes C-0068's ADR-era "zero schema impact" phrase. |
| C-0096 | `context_compaction.maybe_compact(...)` uses `len(text)//4` token estimation, no-ops below threshold, invokes the supplied configured agent runner above threshold, requires a `SYMPHONY_COMPACTED_CONTEXT:` marker, and prepends `<!-- context compacted on ... trimmed N→M tokens -->` to stored compacted context. | `context_compaction.py`, `tests/test_context_compaction.py` | `wiki/analyses/podium-026-context-compaction.md` | high | active | Safe failure raises without returning replacement text. |
| C-0097 | Scheduler dispatch compacts Podium `context_md` before operator prompt rendering and before creating the operator Run row; compaction itself creates no Run row and uses `PodiumTrackerAdapter.replace_context(...)` for replacement semantics. | `scheduler.py`, `tracker_podium.py`, `tests/test_dispatch_compaction.py` | `wiki/analyses/podium-026-context-compaction.md` | high | active | Failure path blocks the Issue and preserves original `context_md`. |
| C-0098 | Podium exposes `POST /api/issues/{id}/compact`, which runs the same compaction flow synchronously and returns `issue_id`, `compacted`, and `token_count`. | `web/api/main.py`, `web/api/tests/test_context_compaction.py` | `wiki/analyses/podium-026-context-compaction.md` | high | active | Endpoint is protected by the existing `/api/*` auth middleware. |
| C-0099 | #027 adds repo-local Podium-era `symphony-*` skill docs plus `skill_migration.py`; `symphony-binding-scaffold` creates a Podium `binding` row and appends a `tracker: podium` binding entry without Plane API or `plane_adapter`. | `.claude/skills/symphony-binding-scaffold/SKILL.md`, `skill_migration.py`, `tests/skills/test_binding_scaffold.py` | `wiki/analyses/symphony-skills-index.md` | high | active | `plane_project_id` remains only as transitional config compatibility while `ProjectBinding` requires it. |
| C-0100 | Migrated `symphony-binding-smoke` and `symphony-bindings-status` now use Podium API paths: `POST /api/bindings/{name}/issues`, `GET /api/issues/{issue_id}/runs`, `GET /api/bindings`, and per-binding Issue reads. | `.claude/skills/symphony-binding-smoke/SKILL.md`, `.claude/skills/symphony-bindings-status/SKILL.md`, `skill_migration.py`, `tests/skills/test_binding_smoke.py`, `tests/skills/test_bindings_status.py` | `wiki/analyses/symphony-skills-index.md` | high | active | Replaces the old Plane ticket/status reads for the migrated suite. |
| C-0101 | `symphony-project-scaffold` is now a deprecated Plane-only alias, `symphony-plane-recover` is Plane-retirement-only, and normal onboarding uses `symphony-binding-scaffold` via `symphony-onboard-project`. | `.claude/skills/symphony-project-scaffold/SKILL.md`, `.claude/skills/symphony-plane-recover/SKILL.md`, `.claude/skills/symphony-onboard-project/SKILL.md`, `tests/skills/test_onboard_project.py`, `tests/skills/test_plane_recover.py` | `wiki/analyses/symphony-skills-index.md` | high | active | Updates C-0049's Plane-era skill-suite picture for Podium. |
| C-0102 | `symphony-workflow-author` is tracker-agnostic: it edits repo `WORKFLOW.md` policy on disk and does not write Podium or Plane. | `.claude/skills/symphony-workflow-author/SKILL.md`, `tests/skills/test_workflow_author.py` | `wiki/analyses/symphony-skills-index.md` | high | active | Applies to both Podium and dormant Plane bindings. |
| C-0103 | #023a installed/enabled the live `podium-api.service` and `podium-web.service` units. API runs uvicorn on `127.0.0.1:8090` with `--workers 1`; web runs Next.js on `127.0.0.1:8091` with `HOST=127.0.0.1`; both load `/home/james/symphony-host.env`, restart on failure, and wire `OnFailure=telegram-alert@%n.service` to `telegram-alert@.service`/`send-telegram-systemd-alert`. Unattended verification checks this wiring without firing a live Telegram alert. | `wiki/raw/podium-api.service`, `wiki/raw/podium-web.service`, `wiki/raw/telegram-alert@.service`, `wiki/raw/send-telegram-systemd-alert`, `.kanban/issues/023a-podium-systemd-units.md` | `wiki/sources/podium-systemd-units.md`, `wiki/analyses/adr-0005-replace-plane-with-podium.md` | high | active | #023a actionable review closed the prior live-alert blocker via wiring verification. The web frontend `HOST=127.0.0.1` was changed to `HOST=10.20.20.16` on 2026-06-12 (#023d, C-0109); the API stays loopback. |

| C-0104 | The `homelab` binding went live on Podium in #023c; both active bindings (`homelab`, `trading`) now declare `tracker: podium`, while the homelab Plane tracker contract is preserved as a commented rollback block until #023d. | `bindings.yml`, `.kanban/issues/023c-podium-homelab-cutover.md`, `wiki/analyses/podium-023c-homelab-cutover.md` | `wiki/analyses/podium-023c-homelab-cutover.md` | high | active | Supersedes C-0083's homelab-on-Plane note. #023d (C-0107) archived only trading and was descoped; homelab Plane project + commented rollback block remain, archive deferred to a follow-up issue. |
| C-0105 | Podium #023c adds infra role columns `approval_required`, `approved`, and `scheduled_for`; `PodiumTrackerAdapter` projects `APPROVAL_REQUIRED`, `APPROVED`, and due `SCHEDULED` roles to those columns, with tests covering each role. | `web/api/schema.py`, `web/api/migrations/versions/0003_infra_role_columns.py`, `tracker_podium.py`, `tests/test_tracker_podium_infra.py`, `wiki/analyses/podium-023c-homelab-cutover.md` | `wiki/concepts/podium-tracker.md` | high | active | Supersedes C-0080's deferred-infra-projection note. |
| C-0106 | The #023c live homelab smoke dispatched through Podium against `/home/james/homelab`, produced issue 18 / run 11 with `state=succeeded`, `verdict=done`, log path, and populated `comments_md`/`context_md`; unattended worker inserted the smoke directly into Podium SQLite because browser UI auth credentials were unavailable. | `.kanban/issues/023c-podium-homelab-cutover.md`, `.kanban/progress.md`, `wiki/analyses/podium-023c-homelab-cutover.md` | `wiki/analyses/podium-023c-homelab-cutover.md` | high | active | Also records stale e2e Todo issues 9–16 parked as Blocked to prevent unintended live dispatch. |

| C-0107 | #023d (descoped to trading-only) archived the trading Plane project (id `201a3995-...`, identifier `TRADING`) on 2026-06-11 via `symphony-plane-recover archive`: `POST .../archive/` → 204, `archived_at: 2026-06-11T22:42:15Z`. James waived the issue's one-week Podium soak gate (trading is a test-only project; gate is operator-owned). The trading `tracker_contract` block — live/uncommented, not commented as the issue assumed — was removed; trading keeps `tracker: podium` + required `plane_project_id` and falls back to `DEFAULT_CONTRACT` (`config.py:391`). | `.kanban/issues/023d-podium-plane-archive.md`, `bindings.yml`, `config.py:345,391`, `wiki/raw/sessions/2026-06-11-podium-023d-trading-plane-archive.md` | `wiki/analyses/podium-023d-trading-plane-archive.md` | high | active | #023d session update. Partially completes C-0059/C-0104's "Plane archived after both cut over"; homelab archive deferred to a follow-up issue. |
| C-0108 | #023d added a "Reverse proxy" section to `web/README.md` documenting the Authelia access-control rule + reverse-proxy forward-auth snippet to expose the Podium frontend through the Authelia gate on `9091`; the snippet uses placeholders and the operator applies the live config (outside this repo). The "Binding tracker rollback" section now records trading's rollback retired (project archived, contract removed) and homelab's retained. | `web/README.md`, `.kanban/issues/023d-podium-plane-archive.md` | `wiki/analyses/podium-023d-trading-plane-archive.md` | high | active | #023d session update. Documents the reverse-proxy posture asserted by C-0065; Authelia/proxy live edit + reachability confirmation remain operator-pending. |
| C-0109 | Operator decision (2026-06-11): Podium is served at FQDN `https://podium.testytech.net`; the reverse proxy targets the Podium frontend at the LAN address `10.20.20.16:8091`, not loopback. This requires `podium-web.service` to listen on the LAN interface (`HOST=10.20.20.16` or `0.0.0.0`) instead of the default `HOST=127.0.0.1` (C-0103), which exposes the unauthenticated port `8091` on the `10.20.20.16` network — the Authelia gate stays the only intended entry, mitigate with a host firewall if needed. The backend API stays loopback. | `web/README.md` (Reverse proxy section), `wiki/raw/sessions/2026-06-11-podium-023d-trading-plane-archive.md`, `/etc/systemd/system/podium-web.service` | `wiki/analyses/podium-023d-trading-plane-archive.md` | high | active | #023d session update. Refines C-0065/C-0103's loopback-only posture for the frontend. **Applied 2026-06-12:** `podium-web.service` set to `HOST=10.20.20.16` (backup `.bak.2026-06-12`), `daemon-reload` + restart; verified `10.20.20.16:8091` → HTTP 200, loopback no longer answers. End-to-end `https://podium.testytech.net` via Authelia still operator-pending. |

| C-0110 | The Podium frontend (`podium-web.service` → `next start`) serves a prebuilt `.next` and does not hot-reload; `next build` overwrites `.next` in place, so rebuilding under the live server makes it serve old in-memory HTML against new on-disk chunk hashes → asset requests 400 with `text/html` MIME and the app hangs at "Checking session…". Fix is `web/frontend/deploy.sh`: build into a staging distDir (via `next.config.mjs` `distDir: process.env.NEXT_DIST_DIR ?? ".next"`) so live `.next` is untouched during the build, then atomic stop→swap (`.next.prev` kept)→start; rollback `mv .next .next.bad && mv .next.prev .next && restart`. `next build` also rewrites `tsconfig.json` (reformat + transient `<distDir>/types` include) — script reverts it. | `web/frontend/deploy.sh`, `web/frontend/next.config.mjs`, `web/frontend/package.json`, `wiki/raw/sessions/2026-06-12-podium-frontend-deploy-and-ui-cosmetics.md` | `wiki/analyses/podium-frontend-deploy-cosmetics.md` | high | active | Session update. Build-only validated (staging distDir produced distinct BUILD_ID, live `.next` untouched); stop/swap/start untested on a real deploy. Changes uncommitted at capture (latest commit `eef75d1`). |
| C-0111 | Podium UI cosmetics (operator-requested): the left sidebar is collapsible via a top-left `PanelLeft` header toggle with open/closed persisted to `localStorage` key `podium:sidebar-open`; the issue card quick-view dropped `PriorityBadge` (low/med/high) and `VerdictPill` (done/review/blocked, duplicated the column) in favour of a colour-coded agent pill (`claude`=orange, `pi`=violet) from `issue.preferred_agent` plus `issue.preferred_model`, falling back to "default agent". `badges.tsx` exports retained (still used by `RunHistoryList`). The card reads the pinned preference, not the last Run's actual agent/model (those are on the `run` table, not the issue-list payload — see C-0058). | `web/frontend/components/AppShell.tsx`, `web/frontend/components/IssueCard.tsx`, `web/frontend/components/badges.tsx` | `wiki/analyses/podium-frontend-deploy-cosmetics.md` | high | active | Session update. Uncommitted at capture. |

| C-0112 | The only `run.updated` WebSocket publish is the API-startup seed publish (`web/api/main.py:126`); `grep -c "run.updated" scheduler.py tracker_podium.py` returns 0 in both. The scheduler is a separate process from the API and writes Run/Issue rows directly to SQLite via `tracker_podium`, never reaching the in-process `WebSocketHub`. So engine-driven Run/Issue state changes are never pushed over the WebSocket — the board/run-detail only refresh on manual refetch or tab-refocus. ADR-0006 decides to surface engine state via gated frontend interval polling (fast ~3s while any Issue is queued/running, slow/off when idle), keeping the WebSocket for optimistic operator-action UI only. | `web/api/main.py:126`, `scheduler.py`, `tracker_podium.py`, `docs/adr/0006-engine-state-surfaced-by-polling-not-websocket.md`, `wiki/raw/adr-0006-engine-state-polling.md` | `wiki/analyses/adr-0006-engine-state-polling.md` | high | active | ADR-0006 ingest; closes the open #020 task in C-0070. Decision reversible via a future scheduler→API notify path. |
| C-0113 | Podium run logs are written once at completion, not incrementally: `agent_runner.py:272` uses `process.communicate(timeout=run_timeout_ms/1000)`, which blocks until pi exits and captures all stdout/stderr at once. A true live log tail would require rewriting the dispatch path and entangles the silent-exit guard (`agent_runner.py:281`) that inspects full stdout/stderr. Runs are bounded by `run_timeout_ms`; orphans are reaped by the #022 restart reconciler (C-0089/C-0090). | `agent_runner.py:262-308` | `wiki/analyses/adr-0006-engine-state-polling.md` | high | active | ADR-0006 ingest. Rationale for choosing an elapsed timer + poll-on-exit over live streaming for run liveness. |

| C-0114 | Podium model choices now come from repo-root `models.yml` rather than hardcoded `KNOWN_MODELS`: `MODELS_PATH` points beside `bindings.yml`, `_load_models()` reads YAML, `_validate_models()` requires a `models` list with non-empty unique `id` values and `agent` in `pi|claude`, and `/api/bindings/{name}/options` returns `models` as `{id, agent, label?, provider?}` objects while degrading missing/invalid catalogs to `[]`. | `models.yml`, `web/api/main.py`, `web/api/tests/test_issue_create.py` | `wiki/analyses/podium-028-model-catalog-searchable-dropdowns.md` | high | active | #028 implementation. Supersedes C-0056's `KNOWN_MODELS` placeholder wording. #032 `symphony-models` should reuse `_validate_models()`. |
| C-0115 | Podium's new-issue modal replaced native select dropdowns with a zero-dependency `FieldCombobox`; model choices filter by selected Agent (`claude` shows `claude-*`, `pi` shows pi models, empty Agent shows all), Agent/Model/Base branch accept free text, and Skill remains selection-only because `preferred_skill` is FK-validated. | `web/frontend/components/NewIssueModal.tsx`, `web/frontend/lib/api.ts`, `web/frontend/tests/new-issue.spec.ts`, `web/api/main.py` | `wiki/analyses/podium-028-model-catalog-searchable-dropdowns.md` | high | active | #028 implementation. Preserves C-0058: `preferred_agent`/`preferred_model` stay free text end to end. |
| C-0116 | Podium root route `/` is now a client-side cross-binding dashboard: it fetches active (non-archived) bindings plus each binding's existing issue-list payload, counts board states from `issue.state`, shows last activity from max `last_event_at`, and lists attention issues where `state=blocked`, `latest_verdict=blocked`, or `latest_run_state=failed`; attention links use `/<binding>?issue=<id>` and `KanbanBoard` opens then clears that flyout deep link. | `web/frontend/app/page.tsx`, `web/frontend/app/[binding]/page.tsx`, `web/frontend/components/KanbanBoard.tsx`, `web/frontend/tests/dashboard.spec.ts` | `wiki/analyses/podium-031-board-overview-dashboard.md` | high | active | #031 implementation; no backend aggregate endpoint added. |
| C-0117 | #032 adds two manual catalog-maintenance skills: `symphony-skills` wraps `python -m web.cli.podium skills refresh` with dry-run → operator confirmation → live refresh and `GET /api/skills` reporting; `symphony-models` edits repo-root `models.yml` directly and lints via shared `_load_models()` / `_validate_models()` rather than bespoke helper code. Both skills explicitly avoid service restarts, Plane calls, env-file reads, direct secret printing, and direct DB edits for model maintenance. | `.claude/skills/symphony-skills/SKILL.md`, `.claude/skills/symphony-models/SKILL.md`, `tests/skills/test_catalog_maintenance_skills.py`, `.kanban/issues/032-symphony-skills-and-models-maintenance-skills.md` | `wiki/analyses/symphony-skills-index.md` | high | active | #032 implementation; extends C-0099..C-0102 with catalog maintenance and operational safety tests. |
| C-0118 | `POST /api/issues/{issue_id}/reply` (auth via global `/api/` middleware) appends an attributed `### Operator Reply (<ISO-timestamp>)` block to `comments_md` AND flips `state` to `todo` AND bumps `updated_at` in one atomic conditional SQL `UPDATE` (`comments_md = COALESCE(comments_md,'') || ?`, guarded by `state IN ('in_review','blocked','done') AND latest_run_state NOT IN ('queued','running')`), then publishes an `issue.updated` WS event. The `todo`-flip is the re-dispatch mechanism — the endpoint only sets `todo`; the next scheduler tick re-runs the agent under existing approval/schedule gates. Returns 409 for `running`/`todo` or active `latest_run_state`, 422 empty body, 400 unknown key, 404 unknown id (`rowcount==0` disambiguated by a follow-up SELECT). No schema change. | `web/api/main.py`, `web/api/tests/test_reply.py`, `tracker_podium.py` | `wiki/concepts/operator-reply.md` | high | active | #operator-reply implementation (`/dev-build` of `plans/feature-operator-reply-comments.md`). Closes the bidirectional Issue Comments gap noted in C-0068. `COALESCE` guards legacy `NULL comments_md` (SQLite `NULL||text` = NULL would drop the reply). |
| C-0120 | Running the Podium frontend e2e suite against the live repo dir clobbers the production build: `pnpm test:e2e` → `playwright test`, whose `webServer` runs `pnpm exec next dev` (`web/frontend/playwright.config.ts:39-44`) with no `NEXT_DIST_DIR`, so it writes a **dev** `.next` (no `BUILD_ID`) into the same `web/frontend/.next` that `podium-web.service`'s `next start` serves. The running `next start` keeps serving from its in-memory build, masking the damage until the next `podium-web` restart — which then crash-loops with `Could not find a production build in the '.next' directory`. A bare restart cannot recover it; only `web/frontend/deploy.sh` (staging-swap production rebuild) restores a valid `.next`. Prevention: keep `test:e2e` out of automated builds against the live dir, or point the e2e `webServer` at a throwaway `NEXT_DIST_DIR`; otherwise always finish with `deploy.sh`. | `web/frontend/playwright.config.ts:39-44`, `web/frontend/package.json`, `web/frontend/deploy.sh` | `wiki/analyses/podium-frontend-deploy-cosmetics.md` | high | active | Observed 2026-06-12 during `/dev-build` of operator-reply: e2e at ~05:04 overwrote `.next`; later `podium-web` restart crash-looped (NRestarts=13); recovered via `deploy.sh`. Extends the in-place-rebuild hazard with a second, stealthier trigger. |
| C-0119 | `prompt_renderer.render_previous_comments_block` gained keyword-only `flag_operator_replies: bool = False`; when `True` (Podium path only, passed by `render_prompt` on the `tracker_kind=="podium"` branch) it appends, after the untrusted-context caveat, a directive that `### Operator Reply` blocks are the operator's directives and the most recent one is the current request to act on, while other comment text stays untrusted. Plane path leaves it `False`, byte-for-byte unchanged. Continuity is transcript re-feed (`comments_md` + `context_md`), not pi session resume (one-shot `--no-session`). | `prompt_renderer.py`, `tests/test_prompt_renderer_podium.py` | `wiki/concepts/operator-reply.md`, `wiki/concepts/prompt-renderer.md` | high | active | #operator-reply implementation. Extends C-0077 (Podium render_prompt path). |

| C-0121 | Symphony has a repo-local Pi personal harness generated at `.pi/extensions/personal-harness.ts` from tracked `wiki/raw/personal-harness-pi-profile.md` plus the local `.rpiv/artifacts/research/2026-06-12_13-25-38_personalize-harness-pi.md` skill artifact. Selected posture: blocking afterWrite syntax checks for Python/JSON/JavaScript/shell, advisory Ruff/frontend tsc/git diff checks, manual-only `uv run pytest -q` and Playwright, and deterministic safety blockers for secrets (including bash shell readers), live services, Plane mutations, generated/runtime data, destructive deletes, and Alembic downgrade. | `wiki/raw/personal-harness-pi-profile.md`, `.rpiv/artifacts/research/2026-06-12_13-25-38_personalize-harness-pi.md`, `.pi/extensions/personal-harness.ts`, `wiki/raw/sessions/2026-06-12-personal-harness-pi.md` | `wiki/analyses/personal-harness-pi-profile.md` | high | active | Generated by `personalize-harness-pi`; run `/reload` in Pi to activate. |
| C-0122 | The generated Pi personal harness passed verification on 2026-06-12: generated marker/default export checks, offline Pi load smoke, isolated offline Pi load smoke, temp-file syntax dry checks, mocked safety regex/path dry checks, mocked project-check trigger/manual scenario/root-resolution checks, and reference-guidance inspection. Project checks remain advisory; no blocking project checks were selected because no clean baseline proof was established. | `wiki/raw/sessions/2026-06-12-personal-harness-pi.md`, `.pi/extensions/personal-harness.ts` | `wiki/analyses/personal-harness-pi-profile.md` | high | active | Verification intentionally avoided reading secrets or touching live services. |

| C-0123 | Design accepted 2026-06-12: Podium issue disposal is a sixth `state` value `archived` — no new column — with archived never an engine Role: the engine never selects archived work, and post-run honors archived as terminal (no verdict state transition, worktree torn down, output discarded). #034 implemented the state value/schema/API/UI; #035 still needs the engine-terminal guard on `transition_state`, which remains an unconditional UPDATE and would otherwise resurrect an archived issue when its in-flight run finishes. | `wiki/raw/sessions/2026-06-12-issue-archive-state-design.md`, `.kanban/issues/034-podium-archived-state-core.md`, `CONTEXT.md`, `tracker_podium.py#315-324` | `wiki/analyses/podium-issue-archive-design.md` | medium | superseded | Superseded by C-0126: #035 implemented the engine-terminal guard and teardown. |
| C-0124 | Design accepted 2026-06-12 (not yet implemented beyond #034's ability to set `state='archived'`): archiving mid-run is allowed — the session runs to completion, worktree teardown is deferred to run completion (`remove_worktree`), and for coding bindings the agent's commits to the bound checkout stay. Archived issues purge after a hardcoded 14 days measured on `updated_at`, swept opportunistically on archive PATCH + API startup, deleting in FK-safe order (null `latest_run_id` → runs → issue) with best-effort run `log_path` unlink. | `wiki/raw/sessions/2026-06-12-issue-archive-state-design.md`, `.kanban/issues/034-podium-archived-state-core.md`, `web/api/worktree.py#83`, `web/api/db.py#51` | `wiki/analyses/podium-issue-archive-design.md` | medium | superseded | Superseded by C-0126 for mid-run teardown implementation and C-0128 for retention purge implementation. |
| C-0125 | #033/#034 implemented the board/UI archive design: Podium board has per-column minimize (−/+ collapse to a narrow strip) on all columns, collapse state persists in localStorage keyed per binding, the archived column renders rightmost and defaults to collapsed, and the archive action is a no-confirm button in IssueFlyout (state chip = restore path); card-hover affordance remains deferred. | `wiki/raw/sessions/2026-06-12-issue-archive-state-design.md`, `.kanban/issues/033-podium-board-column-minimize.md`, `.kanban/issues/034-podium-archived-state-core.md`, `web/frontend/lib/issues.ts`, `web/frontend/components/KanbanBoard.tsx`, `web/frontend/components/IssueFlyout.tsx` | `wiki/analyses/podium-issue-archive-design.md` | high | active | Design decision now implemented for board/UI by #033/#034. |
| C-0126 | #035 implemented archived as engine-terminal: `PodiumTrackerAdapter.transition_state` no-ops for archived issue state writes, PATCH to `state='archived'` tears down idle issue worktrees and branches while leaving queued/running worktrees untouched, and scheduler run completion logs `archived_terminal` then tears down deferred worktrees and skips verdict state transitions after run-row finalization. | `.kanban/issues/035-podium-archive-engine-terminal-contract.md`, `tracker_podium.py`, `web/api/main.py`, `scheduler.py`, `tests/test_tracker_podium.py`, `web/api/tests/test_worktree_api.py`, `tests/test_trading_podium_dispatch.py` | `wiki/analyses/podium-issue-archive-design.md` | high | active | Supersedes C-0123's pending-engine-guard note and C-0124's pending-teardown note. Verification passed: `PATH="$PWD/.venv/bin:$HOME/.local/bin:$PATH" python3 -m pytest -q` (622 passed, 1 skipped) plus fresh Ralph review `PASS`. |
| C-0127 | #036 remains pending for archived issue retention: hard-delete archived issues after 14 days using `updated_at`, sweep on API startup and after archive PATCH, delete in FK-safe order, unlink run logs best-effort, and defensively remove any remaining worktree drift. | `.kanban/issues/036-podium-archived-retention-purge.md`, `wiki/analyses/podium-issue-archive-design.md` | `wiki/analyses/podium-issue-archive-design.md` | high | superseded | Superseded by C-0128: #036 implemented archived retention purge. |
| C-0128 | #036 implemented archived issue retention purge: `PURGE_AFTER_DAYS = 14`, `_purge_archived_issues()` runs at API startup and after archive PATCH transitions, deletes each old archived Issue by clearing `latest_run_id`, deleting Run rows, then deleting the Issue in one transaction, unlinks Run log files best-effort after commit, and removes lingering worktrees based on actual filesystem existence rather than only `worktree_active`. | `.kanban/issues/036-podium-archived-retention-purge.md`, `web/api/main.py`, `web/api/tests/test_archive_purge.py` | `wiki/analyses/podium-issue-archive-design.md` | high | active | Verification passed: `PATH="$PWD/.venv/bin:$HOME/.local/bin:$PATH" python3 -m pytest -q` (633 passed, 1 skipped) plus fresh Ralph review `PASS`. |
| C-0129 | `symphony-restart` and `symphony-troubleshooter` are now repo-local tracked Podium-era operational skills under `.claude/skills/`: restart keeps the explicit James approval gate for `symphony-host.service`, while troubleshooter is read-only and correlates Podium API endpoints, SQLite Issue/Run rows, and scheduler journal evidence. | `.claude/skills/symphony-restart/SKILL.md`, `.claude/skills/symphony-troubleshooter/SKILL.md`, `tests/skills/test_restart_troubleshooter.py` | `wiki/analyses/symphony-skills-index.md` | high | active | Completes project-local consolidation of the Symphony skill suite; stale global/dotfiles `symphony-*` copies were removed in dotfiles commit `06fa9a6`. |

| C-0130 | Symphony has a team-committed Claude Code hook harness at `.claude/settings.json` + four `.claude/hooks/*.sh` scripts (generated by `personalize-harness`): `validate-syntax.sh` (PostToolUse Edit\|Write\|MultiEdit, blocking; py_compile/jq/bash -n), `block-bash-pattern.sh` (PreToolUse Bash, blocking; recursive deletes of `/`/`~`/`$HOME` boundary-anchored so safe absolute deletes pass, plus `pip`/`pipenv`/`poetry` install/add — `uv pip`/`uv add` explicitly allowed), `pre-git-checks.sh` (PreToolUse Bash gated to git commit/push, blocking; `ruff format --check` + `ruff check` on STAGED `*.py` only, then `uv run pytest -q`, each time-boxed 180s), and `reinject-rules.sh` (SessionStart compact, advisory; live-infra invariants). Ruff runs commit-time/changed-files-only because the repo has no `[tool.ruff]` config and a repo-wide check is red (38/82 reformat, 5 lint). Hooks fire only in Claude Code sessions, never in `symphony-host.service`. | `.claude/settings.json`, `.claude/hooks/validate-syntax.sh`, `.claude/hooks/block-bash-pattern.sh`, `.claude/hooks/pre-git-checks.sh`, `.claude/hooks/reinject-rules.sh`, `wiki/raw/sessions/2026-06-12-claude-code-harness.md` | `wiki/analyses/claude-code-harness-profile.md` | high | active | Generated by `personalize-harness` (Claude Code variant). Team-layer, uncommitted at capture. Sibling of the Pi harness (C-0121/C-0122). Originally numbered C-0126 before concurrent #033–#036 work reclaimed that ID. |
| C-0131 | Symphony's runnable test command is `uv run pytest`, NOT bare `python3 -m pytest`: system `/usr/bin/python3` lacks `alembic` and other deps so bare `python3 -m pytest` fails at collection (`ModuleNotFoundError: No module named 'alembic'`), while the uv-managed project venv `/home/james/symphony/.venv` (project-local, not global) has them and `uv run pytest` is green (615 passed, 1 skipped, 53s, exit 0 on 2026-06-12). A `PATH="$PWD/.venv/bin:..." python3 -m pytest` form also works (used by Ralph verification runs). `alembic` is already a declared dep (`pyproject.toml:7` `alembic>=1.17`), locked in `uv.lock`, installed in `.venv` at 1.18.4 — it must NOT be installed into the system interpreter (would create an unpinned copy that drifts from the lockfile). | `pyproject.toml:7`, `uv.lock`, `.venv/bin/python`, `CLAUDE.md`, `wiki/raw/sessions/2026-06-12-claude-code-harness.md` | `wiki/analyses/claude-code-harness-profile.md` | high | active | `CLAUDE.md` "Quick Checks" corrected from `python3 -m pytest` to `uv run pytest` this session. The Pi harness (C-0121) selected `python3 -m pytest -q` and is likely subject to the same env gap unless run with `.venv` on PATH. Originally numbered C-0127 before concurrent #033–#036 work reclaimed that ID. |
| C-0132 | Podium password rotation now has a non-destructive operator helper and README runbook: `scripts/podium-change-password.sh` runs `uv run python -m web.cli.podium set-password`, prints manual env-edit/restart/health-check steps, and intentionally does not edit `/home/james/symphony-host.env` or restart services. `web/README.md` documents replacing only `PODIUM_PASSWORD_HASH`, restarting only `podium-api.service`, and rotating `PODIUM_SESSION_SECRET` only when existing browser sessions should be forced to log in again. | `scripts/podium-change-password.sh`, `web/README.md` | `wiki/analyses/podium-018-auth.md` | high | active | Session update after operator requested making the successful password-change workflow easier. Verified with `bash -n scripts/podium-change-password.sh` and `git diff --check`; no secrets or env contents read. |
| C-0133 | `podium skills refresh --dry-run` prints the full scanned catalog as `name\tdescription\tsource` TSV lines, not a change diff; the `+`/`~`/`-` markers are computed and printed only by the live run inside `_apply_refresh`. `.claude/skills/symphony-skills/SKILL.md` workflow step 2 wrongly describes dry-run output as `+`/`~`/`-` lines. | `web/cli/podium_skills.py:84-85`, `web/cli/podium_skills.py:121-140`, `.claude/skills/symphony-skills/SKILL.md` | `wiki/analyses/podium-skills-catalog-refresh.md` | high | active | Found during first live catalog refresh 2026-06-12. Follow-up: fix SKILL.md wording or make dry-run compute a true diff. Refines (does not contradict) C-0117's "dry-run preview" description. |
| C-0134 | Skill catalog refresh is a single-source contract: default scan source is `~/.claude/skills` (`DEFAULT_SOURCE`; on aidev a symlink to `/home/james/dotfiles/.claude/skills`), repo-local `/home/james/symphony/.claude/skills/` `symphony-*` skills are NOT cataloged, and each run deletes every file-backed row absent from its own scan — so two runs with different `--source` values clobber each other. Cataloging both dirs needs a combined source or a code change. | `web/cli/podium_skills.py:19`, `web/cli/podium_skills.py:135-141`, `web/cli/podium.py` | `wiki/analyses/podium-skills-catalog-refresh.md` | high | active | Decision pending: whether symphony-* skills belong in the Podium Skill dropdown. |
| C-0135 | `issue.preferred_skill REFERENCES skill(name)` blocks the refresh's stale-row DELETE with `sqlite3.IntegrityError: FOREIGN KEY constraint failed`, aborting the whole refresh with a clean rollback (commit only after `_apply_refresh`; no partial writes, verified live). Manual-row protection (`source=''`) covers deletion only — the upsert overwrites any existing row whose name matches a scanned skill, which converted the manual `diagnose` row to file-backed on 2026-06-12. | `web/api/schema.py`, `web/cli/podium_skills.py:124-141`, `wiki/raw/sessions/2026-06-12-podium-skills-catalog-refresh.md` | `wiki/analyses/podium-skills-catalog-refresh.md` | high | active | Hit live on legacy `/diagnose` seed row referenced by 12 e2e issues (homelab, ids 5-16); James approved repointing them to `diagnose` before rerun. Follow-up: graceful FK handling and/or upsert protection for manual rows. |
| C-0136 | First live catalog refresh (2026-06-12) brought the Podium `skill` table to 50 rows: 44 added from dotfiles, 4 updated (`blueprint`, `code-review`, `diagnose`, `tdd`), legacy `/diagnose` seed row removed; manual rows `catalog-alpha`/`catalog-bravo` untouched. Removal is durable because `web/api/seed.py` no longer contains `_seed_skills`/`SEED_SKILLS` — seeding covers bindings/issues/runs only, gated on an empty `binding` table; the refresh CLI owns the `skill` table. | `web/api/seed.py`, `wiki/raw/sessions/2026-06-12-podium-skills-catalog-refresh.md`, `tests/skills/test_catalog_maintenance_skills.py` | `wiki/analyses/podium-skills-catalog-refresh.md` | high | active | Supersedes C-0055. Post-run verification: zero pending diff vs scan; catalog maintenance skill tests 6 passed. Same evening: `catalog-alpha`/`catalog-bravo` identified as leaked Playwright e2e fixtures (older `seedSkills` wrote `source=''` to live DB; `web/frontend/tests/skill-catalog.spec.ts`) and deleted — catalog now 48 rows, zero manual rows. Current `fixtures.ts` isolates via `PODIUM_DB_PATH` → `web/test-results/podium-e2e.db` and tags `source='e2e'`, which refresh would auto-delete if leaked. |

| C-0137 | #038 adds `POST /api/issues/{issue_id}/dismiss`: it is protected by the existing `/api/*` auth middleware, returns 404 for missing Issues, performs a guarded SQL `UPDATE` only for `state IN ('in_review','blocked')`, sets `inbox_dismissed_at` plus a monotonic `updated_at`, returns the updated Issue row, and publishes `issue.updated` over the WebSocket hub. Non-Inbox states return 409. | `web/api/main.py`, `web/api/tests/test_inbox.py`, `.kanban/issues/038-podium-inbox-dismiss-resurface.md` | `wiki/analyses/podium-038-inbox-dismiss-resurface.md` | high | active | #038 implementation and Ralph review passed. |
| C-0138 | #038 makes Inbox resurface transition-authoritative: PATCH setting `state` to `in_review`/`blocked`, `PodiumTrackerAdapter.transition_state()` into those states, orphan-run reconciliation to `blocked`, and the API blocked helper all clear `inbox_dismissed_at`; transitions into other states preserve dismissal. Newer `last_event_at` remains a secondary resurface path via the #037 Inbox query filter. | `web/api/main.py`, `tracker_podium.py`, `web/api/tests/test_inbox.py`, `tests/test_tracker_podium.py`, `.kanban/issues/038-podium-inbox-dismiss-resurface.md` | `wiki/analyses/podium-038-inbox-dismiss-resurface.md` | high | active | Covers blocked-reconciler/no-new-last_event_at edge from issue #038. |
| C-0139 | The Sidebar Inbox card dismiss UX is optimistic and state-preserving: a hover/focus check button outside the navigation Link calls `dismissIssue(id)`, removes the row from TanStack Query `['inbox']`, restores the previous cache on error, invalidates `['inbox']` when settled, and leaves the Issue visible in its board column. | `web/frontend/components/Sidebar.tsx`, `web/frontend/lib/api.ts`, `web/frontend/tests/inbox.spec.ts` | `wiki/analyses/podium-038-inbox-dismiss-resurface.md` | high | active | Playwright coverage verifies no navigation, card removal, and board persistence. |

Claim IDs use the next available zero-padded integer in `C-0001` format. Before adding claims, scan existing `C-####` IDs in this file, find the maximum, and increment by one for each new claim.
