# Claims Registry — Cold Archive

Demoted low-traffic claims. NOT loaded by default; search explicitly.
| ID | Kind | Claim | Source | Page | Confidence | Status | Created | Hits | Superseded | Impact | Notes |
|---|---|---|---|---|---|---|---|---|---|---|---|
| C-0005 |  | A Binding whose repo has no readable `WORKFLOW.md` is a hard config error — Symphony refuses to dispatch and posts a blocked comment. | `wiki/raw/symphony-context.md#24` | `wiki/concepts/symphony-engine.md` | high | cold |  | 0 |  |  | First ingest. Superseded by C-0203 (ADR-0011, 2026-06-14): holds for **infra** bindings only; **coding** bindings ignore `WORKFLOW.md` and dispatch without one. |
| C-0015 |  | Telegram notifications fire on IN_REVIEW and BLOCKED transitions only; scheduled and released ticket notifications are intentionally disabled. Failures log warnings, never block scheduler. | `wiki/raw/runbook-symphony.md#199-208` | `wiki/concepts/symphony-operations.md` | high | cold |  | 0 |  |  | Batch ingest. Superseded by C-0306: issue-transition Telegram notifications are now disabled by default and require explicit opt-in. |
| C-0023 |  | trading Binding omits the SCHEDULED and APPROVED Roles, so the scheduled-release and plan-approval paths are inactive for that project. | `wiki/raw/bindings.yml` | `wiki/entities/binding-trading.md` | high | cold |  | 0 |  |  | Batch ingest. Superseded by C-0107: #023d removed trading's entire `tracker_contract` block (2026-06-11), so trading now resolves to `DEFAULT_CONTRACT` rather than a leaner hand-tuned Role set. The cited `wiki/raw/bindings.yml` snapshot predates the removal. |
| C-0029 |  | trading WORKFLOW.md requires explicit approval **in the Plane issue body** naming the exact live action before any live trading, exchange API, real order, real portfolio, production Temporal namespace, or long-running agent action; otherwise sandbox-only work. | `wiki/raw/workflow-trading.md#23-26` | `wiki/entities/workflow-trading.md` | high | cold |  | 0 |  |  | Tier 1. Superseded by C-0203 (ADR-0011, 2026-06-14): trading is a `coding` binding, so Symphony no longer reads its WORKFLOW.md; the file was deleted. Safety is now the repo's responsibility via native agent config. The raw snapshot predates deletion. |
| C-0030 |  | trading WORKFLOW.md plan-artifact path is `Plans/<identifier>.md` (capital P, identifier not slug) — different convention from homelab's `plans/<slug>.md`. | `wiki/raw/workflow-trading.md#17-18` | `wiki/entities/workflow-trading.md` | high | cold |  | 0 |  |  | Tier 1. Superseded by C-0203 (ADR-0011, 2026-06-14): trading WORKFLOW.md deleted and no longer read for `coding` bindings. |
| C-0039 |  | scheduler.py CLAIM_PREFIX is `"Symphony claimed at "` and appends `code_sha=<sha>` after the timestamp; backwards-compatible because _claimed_at splits on CLAIM_PREFIX and takes the first whitespace token. | `scheduler.py#58-65` | `wiki/concepts/scheduler-loop.md` | high | cold |  | 0 |  |  | Tier 2. Superseded by C-0162 (#046): the `Symphony claimed at` claim comment is no longer posted and `_CODE_SHA`/the `code_sha=` append were removed; claim time now reads from the Run record `started_at`. `CLAIM_PREFIX` and the comment-parse path remain only as a Plane/historical fallback. |
| C-0051 |  | Plane 429 cooldown is now shared across binding dispatch states via `_PLANE_COOLDOWN_UNTIL`, while retaining per-binding cooldown state. | `scheduler.py#57,117-173`, `tests/test_scheduler.py#3964-3990`, `wiki/raw/sessions/2026-06-09-trading-smoke-rate-limit.md` | `wiki/concepts/scheduler-loop.md` | high | cold |  | 0 |  |  | Superseded by C-0232: Issue #069 removed `_PLANE_COOLDOWN_UNTIL`; cooldown is per `_DispatchState` only. |
| C-0055 |  | Podium skill seeding is per-row `INSERT OR IGNORE` over `SEED_SKILLS` (now incl. `/diagnose`) at every boot — it resurrects deleted seed rows and never rewrites descriptions, so #015's catalog refresh must retire `_seed_skills` or own the table. | `web/api/seed.py`, `.kanban/issues/015-podium-skill-catalog.md` | `wiki/analyses/podium-014-new-issue-flow.md` | high | cold |  | 0 |  |  | Session update; commit `a68cccf`. Superseded by C-0136: `_seed_skills`/`SEED_SKILLS` were removed from `web/api/seed.py`; the refresh CLI now owns the `skill` table and removed seed rows stay removed across restarts. |
| C-0056 |  | Podium `GET /api/bindings/{name}/options` serves dropdown choices: agents statically mirror the scheduler's `_validate_agent` set (`pi`, `claude`; `config.py:471`), models are the curated `KNOWN_MODELS` placeholder (no catalog source of truth exists), branches come live from `git for-each-ref` against the binding's `repo_path` and degrade to `[]` on failure. | `web/api/main.py`, `wiki/raw/sessions/2026-06-10-podium-014-new-issue-flow.md` | `wiki/analyses/podium-014-new-issue-flow.md` | high | cold |  | 0 |  |  | Session update; commit `bf7cfd0`. Superseded by C-0114: #028 removed `KNOWN_MODELS` and made `models.yml` the model catalog source of truth. |
| C-0058 |  | `issue.preferred_agent` and `preferred_model` are free text end to end (only `preferred_skill` is FK-validated); #020 dispatch must fall back to binding defaults on unknown values. | `web/api/main.py`, `.kanban/issues/020-podium-trading-cutover.md` | `wiki/analyses/podium-014-new-issue-flow.md` | high | cold |  | 0 |  |  | Session update. Superseded by C-0141/C-0142: dispatch now validates preferred_model against models.yml and blocks unknown values (2026-06-12). |
| C-0066 |  | Known bug: `scheduler.py:488` keys `is_coding` off `config.bindings[0].binding_type` alone, so the whole `is_coding` branch resolves against whichever binding is first in `bindings.yml` rather than per-issue. Flagged in ADR-0005 for cleanup, out of ADR scope. | `scheduler.py:488`, `wiki/raw/adr-0005-replace-plane-with-podium.md#3` | `wiki/analyses/adr-0005-replace-plane-with-podium.md` | high | cold |  | 0 |  |  | ADR-0005 ingest. Superseded by C-0172 (2026-06-13): `is_coding` now keys off the per-tick binding at `scheduler.py:948`, not `config.bindings[0]`. |
| C-0068 |  | Podium keeps `comments_md` (bidirectional human↔AI thread: humans write fully, AI writes concise summaries) and `context_md` (AI-only session log: AI writes full output, AI reads on dispatch) as separate Issue blobs. When `context_md` exceeds threshold, Symphony invokes the configured agent with a hardcoded compaction prompt — engine-built compaction, not a Skill, zero schema impact. | `wiki/raw/adr-0005-replace-plane-with-podium.md#5`, `web/api/schema.py:34-35` | `wiki/analyses/adr-0005-replace-plane-with-podium.md` | high | cold |  | 0 |  |  | ADR-0005 design-era claim. Superseded by C-0095: implementation added `binding_settings` schema for threshold/keep values, so "zero schema impact" is no longer true. |
| C-0080 |  | `tracker_podium.py` implements the runtime `TrackerAdapter` surface against SQLite without a direct `plane_adapter` import; coding-binding Roles project to `issue.state`, `preferred_skill`, and `preferred_agent`, while approval/scheduled infra projection remains deferred to #023c. | `tracker_podium.py`, `tracker_adapter.py`, `tests/test_tracker_podium.py` | `wiki/concepts/podium-tracker.md` | high | cold |  | 0 |  |  | #019 implementation; direct Plane import absence verified by grep in review. Superseded by C-0105: #023c implemented infra approval/schedule columns and projection. |
| C-0095 |  | Podium #026 implements Issue Context compaction with a new `binding_settings` table containing `context_compact_threshold_tokens` (default 16000) and `context_compact_keep_recent_runs` (default 3), via Alembic revision `0002_context_compaction_settings`. | `web/api/schema.py`, `web/api/migrations/versions/0002_context_compaction_settings.py`, `tests/test_alembic_baseline.py` | `wiki/analyses/podium-026-context-compaction.md` | high | cold |  | 0 |  |  | Supersedes C-0068's ADR-era "zero schema impact" phrase. **Superseded by C-0244 (2026-06-18): compaction retired. The `binding_settings.context_compact_*` columns + revision `0002` survive but are inert/dormant.** |
| C-0096 |  | `context_compaction.maybe_compact(...)` uses `len(text)//4` token estimation, no-ops below threshold, invokes the supplied configured agent runner above threshold, requires a `SYMPHONY_COMPACTED_CONTEXT:` marker, and prepends `<!-- context compacted on ... trimmed N→M tokens -->` to stored compacted context. | `context_compaction.py`, `tests/test_context_compaction.py` | `wiki/analyses/podium-026-context-compaction.md` | high | cold |  | 0 |  |  | Safe failure raises without returning replacement text. **Superseded by C-0244 (2026-06-18): `context_compaction.py` and `tests/test_context_compaction.py` were deleted.** |
| C-0097 |  | Scheduler dispatch compacts Podium `context_md` before operator prompt rendering and before creating the operator Run row; compaction itself creates no Run row and uses `PodiumTrackerAdapter.replace_context(...)` for replacement semantics. | `scheduler.py`, `tracker_podium.py`, `tests/test_dispatch_compaction.py` | `wiki/analyses/podium-026-context-compaction.md` | high | cold |  | 0 |  |  | Failure path blocks the Issue and preserves original `context_md`. **Superseded by C-0244 (2026-06-18): `scheduler/__init__._maybe_compact_context` and `tracker_podium.replace_context` removed; no pre-dispatch compaction occurs and `context-compaction-failed` is unreachable.** |
| C-0098 |  | Podium exposes `POST /api/issues/{id}/compact`, which runs the same compaction flow synchronously and returns `issue_id`, `compacted`, and `token_count`. | `web/api/main.py`, `web/api/tests/test_context_compaction.py` | `wiki/analyses/podium-026-context-compaction.md` | high | cold |  | 0 |  |  | Endpoint is protected by the existing `/api/*` auth middleware. **Superseded by C-0244 (2026-06-18): the `/compact` route + `_compact_issue_context` were removed; POSTing it now returns 404/405.** |
| C-0121 |  | Symphony has a repo-local Pi personal harness generated at `.pi/extensions/personal-harness.ts` from tracked `wiki/raw/personal-harness-pi-profile.md` plus the local `.rpiv/artifacts/research/2026-06-12_13-25-38_personalize-harness-pi.md` skill artifact. Selected posture: blocking afterWrite syntax checks for Python/JSON/JavaScript/shell, advisory Ruff/frontend tsc/git diff checks, manual-only `uv run pytest -q` and Playwright, and deterministic safety blockers for secrets (including bash shell readers), live services, Plane mutations, generated/runtime data, destructive deletes, and Alembic downgrade. | `wiki/raw/personal-harness-pi-profile.md`, `.rpiv/artifacts/research/2026-06-12_13-25-38_personalize-harness-pi.md`, `.pi/extensions/personal-harness.ts`, `wiki/raw/sessions/2026-06-12-personal-harness-pi.md` | `wiki/analyses/personal-harness-pi-profile.md` | high | cold |  | 0 |  |  | Superseded by C-0237: `.pi/extensions/personal-harness.ts` was deleted on 2026-06-17 at James's request. Historical profile/source retained. |
| C-0122 |  | The generated Pi personal harness passed verification on 2026-06-12: generated marker/default export checks, offline Pi load smoke, isolated offline Pi load smoke, temp-file syntax dry checks, mocked safety regex/path dry checks, mocked project-check trigger/manual scenario/root-resolution checks, and reference-guidance inspection. Project checks remain advisory; no blocking project checks were selected because no clean baseline proof was established. | `wiki/raw/sessions/2026-06-12-personal-harness-pi.md`, `.pi/extensions/personal-harness.ts` | `wiki/analyses/personal-harness-pi-profile.md` | high | cold |  | 0 |  |  | Historical verification of the removed extension; superseded for current project state by C-0237. |
| C-0123 |  | Design accepted 2026-06-12: Podium issue disposal is a sixth `state` value `archived` — no new column — with archived never an engine Role: the engine never selects archived work, and post-run honors archived as terminal (no verdict state transition, worktree torn down, output discarded). #034 implemented the state value/schema/API/UI; #035 still needs the engine-terminal guard on `transition_state`, which remains an unconditional UPDATE and would otherwise resurrect an archived issue when its in-flight run finishes. | `wiki/raw/sessions/2026-06-12-issue-archive-state-design.md`, `.kanban/issues/034-podium-archived-state-core.md`, `CONTEXT.md`, `tracker_podium.py#315-324` | `wiki/analyses/podium-issue-archive-design.md` | medium | cold |  | 0 |  |  | Superseded by C-0126: #035 implemented the engine-terminal guard and teardown. |
| C-0124 |  | Design accepted 2026-06-12 (not yet implemented beyond #034's ability to set `state='archived'`): archiving mid-run is allowed — the session runs to completion, worktree teardown is deferred to run completion (`remove_worktree`), and for coding bindings the agent's commits to the bound checkout stay. Archived issues purge after a hardcoded 14 days measured on `updated_at`, swept opportunistically on archive PATCH + API startup, deleting in FK-safe order (null `latest_run_id` → runs → issue) with best-effort run `log_path` unlink. | `wiki/raw/sessions/2026-06-12-issue-archive-state-design.md`, `.kanban/issues/034-podium-archived-state-core.md`, `web/api/worktree.py#83`, `web/api/db.py#51` | `wiki/analyses/podium-issue-archive-design.md` | medium | cold |  | 0 |  |  | Superseded by C-0126 for mid-run teardown implementation and C-0128 for retention purge implementation. |
| C-0127 |  | #036 remains pending for archived issue retention: hard-delete archived issues after 14 days using `updated_at`, sweep on API startup and after archive PATCH, delete in FK-safe order, unlink run logs best-effort, and defensively remove any remaining worktree drift. | `.kanban/issues/036-podium-archived-retention-purge.md`, `wiki/analyses/podium-issue-archive-design.md` | `wiki/analyses/podium-issue-archive-design.md` | high | cold |  | 0 |  |  | Superseded by C-0128: #036 implemented archived retention purge. |
| C-0144 |  | Global run timeout is 60 min (`config.py` default `3_600_000`; both binding WORKFLOW.md frontmatters updated; frontmatter is decorative for the subprocess kill) and per-issue `max_duration_seconds` was dropped via migration `0006_drop_max_duration_seconds`. | `config.py`, `web/api/migrations/versions/0006_drop_max_duration_seconds.py`, `wiki/raw/sessions/2026-06-12-issue-dispatch-contract.md` | `wiki/analyses/podium-issue-dispatch-contract.md` | high | cold |  | 0 |  |  | Session update. Superseded by C-0307: default timeout is now 2 hours / `7_200_000` ms. |
| C-0146 |  | `preferred_agent: claude` blocks at the dispatch gate ("claude engine is not wired") until a real Claude adapter is wired; kanban `#040`/`#042` specify the tmux-send-keys adapter contract (marker parity, honest run rows, pi path unchanged). | `scheduler.py`, `.kanban/issues/040-claude-engine-adapter.md`, `wiki/raw/sessions/2026-06-12-issue-dispatch-contract.md` | `wiki/analyses/podium-issue-dispatch-contract.md` | high | cold |  | 0 |  |  | Superseded by C-0152: #043 removed the non-Pi dispatch block and routes matching Claude Issues through `ClaudeAgentAdapter`. Continues C-0016's historical claude-paused lineage. |
| C-0203 |  | `WORKFLOW.md` is **infra-only** (ADR-0011, 2026-06-14): `render_prompt` skips `load_workflow` when `binding_type == "coding"` and assembles the prompt from the issue (+ comments/context on the re-feed floor) + the renderer-constant `OUTPUT_CONTRACT` only — coding bindings never read `WORKFLOW.md` (ignored, not optional), present or absent. Infra bindings still require it (a missing file is still a hard `workflow-missing` block). Verdict scraping survives because `OUTPUT_CONTRACT` is independent of `WORKFLOW.md`, and the `WorkflowConfig` frontmatter (`poll_interval_ms`/`run_timeout_ms`) was already dead (discarded by `render_prompt`; runtime timeouts come from `SymphonyConfig`). `WORKFLOW.md` was deleted from the `symphony` and `trading` repos (code change landed first to avoid `workflow-missing`). | `prompt_renderer.py`, `docs/adr/0011-workflow-md-infra-only.md`, `tests/test_prompt_renderer_podium.py`, `config.py:131-132` | `wiki/analyses/adr-0011-workflow-md-infra-only.md` | high | cold |  | 0 |  |  | Session update (grill-me). Supersedes C-0005 (workflow mandatory for *every* binding); supersedes C-0029/C-0030 (trading WORKFLOW.md, file deleted). **File-based half superseded by C-0276 (ADR-0016, 2026-06-20): infra no longer requires a `WORKFLOW.md`; the contract becomes a renderer constant, no more `workflow-missing` for infra. ADR-0011's coding-ignore `binding_type` split + OUTPUT_CONTRACT independence remain true. Original ADR-0011 code committed; restart was pending.** |
| C-0204 |  | `WORKFLOW.md` is **autonomy policy, not safety** (ADR-0011): it instructs the agent on how to operate autonomously under Symphony's orchestration (infra patrol lifecycle, plan/build/execute, completion contract). Safety rules and repo conventions are the bound repo's responsibility, expressed in its native agent config (`CLAUDE.md`/`AGENTS.md`) and owned by whoever manages the project — Symphony does not enforce safety and stays strict to dispatch/verdict/autonomy-lifecycle. The most Symphony does is *flag* (warn, never block) at bind time when a coding repo lacks native agent config. `binding_type` value names stay `coding`/`infra`; "cli" is product framing only. | `docs/adr/0011-workflow-md-infra-only.md`, `CONTEXT.md`, `.claude/skills/symphony-onboard-project/SKILL.md`, `.claude/skills/symphony-workflow-author/SKILL.md` | `wiki/analyses/adr-0011-workflow-md-infra-only.md` | high | cold |  | 0 |  |  | Session update (grill-me). Reframes the Workflow glossary term. **Superseded by C-0277 (ADR-0016, 2026-06-20): post-ADR-0016 the `WORKFLOW.md` contract is engine boilerplate (renderer constant), NOT "autonomy policy" — autonomy joins safety in the host `CLAUDE.md`. The "safety is the repo's job" stance is unchanged.** |
| C-0224 |  | **A layer-by-layer architecture review of the root scheduler module exists** at `.rpiv/artifacts/architecture-reviews/2026-06-16_22-42-19_root-scheduler-module.md` (git-ignored): 8 layers, 41 findings (29 accepted / 5 rejected / 7 deferred), 2 methodology principles (M1 keep tested zero-consumer primitives that pair with an existing flow; M2 verify an unusual coupling's reason before fixing it), 8 themes, a 7-phase blueprint-consumable plan. Highest-leverage threads: T4 scheduler decomposition (`run_tick` ~882 LOC at `scheduler.py:1168-2050`; `scheduler.py` 3039 LOC), T7 tracker-vocabulary-home (`CandidateIssue`/`TrackerAdapter` Protocol live inside `plane_adapter.py` and are duplicated). Implementation NOT started. | `.rpiv/artifacts/architecture-reviews/2026-06-16_22-42-19_root-scheduler-module.md`, `scheduler.py`, `plane_adapter.py`, `tracker_podium.py` | `wiki/analyses/root-scheduler-architecture-review.md` | high | cold |  | 0 |  |  | Superseded by C-0226: implementation began with Issue #45's small polish batch. The artifact and plan still exist; only the "implementation not started" status is stale. |
| C-0225 |  | **Symphony ships the Plane-only `plane` callback CLI and injects `SYMPHONY_PLANE_API_KEY` + Plane callback env into every dispatched agent subprocess** (`agent_runner._agent_env:213-218`; helper copied in `run_agent:285`/`run_remote_agent:454`/`run_pi_rpc_agent:600`), even though all live bindings are Podium (C-0223) and Podium agents drive state purely via SYMPHONY_RESULT markers. This is a present secret-exposure surface + dead weight. Review finding L6-02 (sharpened by the Pi meta-review) splits the fix: de-shipping the Plane secret/helper to Podium agents is a near-term **accepted Phase 5** cleanup; full `plane_cli` deletion stays deferred (theme T8, gated on Plane sunset). | `agent_runner.py`, `plane_cli.py`, `bindings.yml` | `wiki/analyses/root-scheduler-architecture-review.md` | high | cold |  | 0 |  |  | Superseded by C-0230: Issue #067 implemented the near-term de-shipping cleanup for Podium local/RPC/remote agents while preserving Plane binding back-compat. Full `plane_cli` deletion remains deferred. |
| C-0247 |  | **Silent-discard gap in worktree merge-on-done.** When the agent leaves work uncommitted in the worktree, marking the Issue `done` makes `git merge --ff-only` a no-op "already up to date" (branch == base), then `cleanup_worktree` → `git worktree remove --force` silently deletes the uncommitted work while the Issue shows green `done` with nothing landed. This is the only silent-data-loss path in the feature; worst for the `symphony` self-binding whose worktree holds live scheduler changes. | `web/api/worktree.py#83-96,116-174`, `web/api/main.py#1339-1347` | `wiki/analyses/analysis-session-worktree-done-commit-redispatch.md` | high | cold |  | 0 |  |  | Session-update (grill-me), verified by code reading. **Superseded 2026-06-18 by C-0250 — gap closed: a dirty worktree at `done` now re-dispatches (or blocks at cap), never `git worktree remove --force` of uncommitted work.** Retained for history. |
| C-0248 |  | **ADR-0014 (proposed, not implemented): re-dispatch the agent to commit before merging at done-time.** At `state -> done`, classify the worktree: clean + commits ahead → FF-merge + teardown (unchanged); no commits ahead OR dirty working tree → synthesize an `### Operator Reply` instructing `git add -A && git commit` and flip `state='todo'` (reusing the operator-reply re-dispatch path), so the agent resumes in its own persistent dirty worktree and commits. Loop guard: re-dispatch at most twice (counted via the synthetic commit-note marker in `comments_md`, no schema change), then fall back to `blocked` — never auto-commit un-agent-committed work into `main`. Non-FF block path unchanged. Rejected alternatives: immediate block (friction), `git add -A` auto-commit (bypasses pre-commit test obligation, risks landing broken code), status-quo silent force-remove (data loss). | `docs/adr/0014-worktree-done-commit-redispatch.md`, `web/api/main.py#1074-1113`, `prompt_renderer.py#278-290`, `web/api/worktree.py#31-48`, `wiki/raw/sessions/2026-06-18-worktree-done-commit-redispatch-design.md` | `wiki/analyses/analysis-session-worktree-done-commit-redispatch.md` | high | cold |  | 0 |  |  | Decision accepted by James 2026-06-18; ADR status `proposed`; build deferred. **Superseded 2026-06-18 by C-0250 (implemented): the shipped predicate is dirty-only, not "no commits ahead OR dirty" — a clean empty worktree falls through to the no-op merge+teardown. ADR now `accepted`.** Retained for the proposed-design record. |
| C-0261 |  | **Infra Build plan-path resolution uses the Podium numeric identifier, so the plan file must be `plans/{id}.md` — not `plans/{id}-{title}.md`.** `_issue_slug` slugs `issue.identifier or issue.id` (`scheduler/__init__.py:883`); the Podium adapter sets `identifier = str(issue.id)` (`tracker_podium.py:95`), so for issue 59 the slug is `59` and `_validated_fallback_plan_path` expects `<repo>/plans/59.md` (`scheduler/__init__.py:889,1251`). A Plan run that writes the old Plane-era `{id}-{title}` name (`plans/59-temporal.md`) fails `_validate_issue_plan_path` ("plan path does not match the current issue slug"), so Build posts "no readable plan file was found", flips `build→plan`, and returns the issue to Todo — looping every poll until the filename is corrected. Refines C-0030's `plans/<slug>.md` convention for the Podium era. | `scheduler/__init__.py:883-921,1251`, `tracker_podium.py:95` | `wiki/concepts/symphony-engine.md` | high | cold |  | 0 |  |  | Session 2026-06-19, homelab issue 59 "Temporal". Immediate unblock: renamed `plans/59-temporal.md`→`plans/59.md` (homelab commit `f6b9002`). Resolved by C-0262: Build now tolerates both naming conventions. |
| C-0281 |  | **Residual (deferred): Podium's `/reply` reopens to `todo` on EVERY comment, so patrol pass/close comments re-dispatch pi each cycle and a close can be clobbered back to `in_review` by an in-flight pi run; durable fix = a non-reopening comment endpoint.** `/reply` is the ONLY Podium comment mechanism (`tracker_podium.py` folds comments into one `comments_md` blob; `wiki/concepts/operator-reply.md`) but it always flips `state='todo'`. The C-0279/C-0280 reorder+tolerate-409 fix stops the workflow failures and makes the close *flip* stick, but does NOT stop the reopen side effect: a below-threshold `pass-recorded` issue is reopened by its own pass comment → scheduler dispatches pi again; and a close can be overridden if a pi run dispatched during that churn completes after the DONE flip (run completion moves `done`→`in_review`). Observed live: issue 62 `done`→`in_review` ~20s after close while an in-flight run drained (issue 63, no in-flight run, stayed `done`). Operator chose the reorder fix and **deferred** the durable fix: add a podium-api endpoint that appends a comment WITHOUT the reopen/state-gate, and route bot status comments (failure/pass/close) through it. | `web/api/main.py:1135-1158` (reply reopen semantics), `automation/homelab-stack/src/homelab_router/podium_adapter.py` (`add_comment`→`_reply_path`), `podium.db` issue 62 (`done`→`in_review`), `wiki/raw/sessions/2026-06-20-patrol-podium-reply-409-and-close-stickiness.md` | `wiki/analyses/adr-0015-patrol-podium-tracker-adapter.md` | medium | cold |  | 0 |  |  | Session 2026-06-20. Open follow-up; deferred design decision. Durable fix touches podium-api (excluded service) — operator-gated. **Superseded 2026-06-20 by C-0285: the deferred non-reopening comment endpoint (ADR-0017) is now built, deployed, and live-verified — the reopen-on-comment churn is resolved. Retained for the deferred-design record.** |

Claim IDs use the next available zero-padded integer in `C-0001` format. Schema and bounding gates enforced by `gate.py`.
