# Session Capture: ADR-0018 #098 landing, schedule-UI refinement, and window deploy-verify

- Date: 2026-06-21
- Purpose: Land the homelab policy half of ADR-0018 (#098), simplify the schedule UI, deploy the full block→schedule path, and verify it on a real maintenance window.
- Scope: Durable facts/decisions for the #098 implementation, the schedule-UI changes, and the deploy-verify outcome (a real problem found). Excludes routine deploy chatter and unrelated working-tree drift.

## Durable Facts

- #098 landed (homelab repo, commit `f76f7ab`): block→schedule policy in `/home/james/homelab/CLAUDE.md`; `record_failure` dedup-don't-clobber (evidence-only on a scheduled hold); `record_pass` close clears `scheduled_for`; new `TicketWriter.is_scheduled_hold` + `update_issue(clear_schedule=)`; `InMemoryPodiumTransport` carries `scheduled_for` + a `.schedule()` helper; `TestScheduleHold` regression suite. Detection is by Podium fields (`scheduled_for` + `state=="todo"`, or latest `Symphony-Schedule:` control line in `comments_md`), never labels. PlaneAdapter `is_scheduled_hold` returns False (window scheduling is Podium-only). — Evidence: homelab `f76f7ab`; `automation/homelab-stack/src/homelab_router/podium_adapter.py`, `.../homelab_worker/patrol_plane.py`, `.../tests/test_patrol_plane.py`. Verified: `uv run pytest` 732 passed.
- The schedule clear-on-close uses a plain `PATCH {state:done, scheduled_for:null}` — the podium-api PATCH whitelist accepts `scheduled_for=null` (`web/api/main.py` patch handler; `web/api/tests/test_issue_patch.py:27`), so no DELETE primitive was added.
- Schedule UI was simplified (symphony commits `002b25f`, `9c21520`, `4047704`, `7e4a319`): `ScheduleControl` is now a 2-option Yes/No dropdown (Yes = `next_window`, No = unscheduled); Custom date-time + Reason inputs removed (default reason used). The flyout no longer uses the full-width card — schedule is an inline `ScheduleChip` in the metadata chip row that applies on change (Yes → `POST /schedule`, No → `DELETE /schedule`). Scheduling optimistically sets `state:"todo"` so the card moves to To Do immediately (mirrors the endpoint). A scheduled (held, `todo`) issue can be annotated via the append-only `/comment` primitive — new `postComment` client + composer "comment mode" (no reopen/re-dispatch). `NewIssueModal` still uses `ScheduleControl` (draft read at submit). — Evidence: `web/frontend/components/ScheduleControl.tsx`, `web/frontend/components/IssueFlyout.tsx`, `web/frontend/lib/api.ts`, `web/frontend/tests/schedule.spec.ts` (4 Playwright tests pass).
- Stale-deploy pattern: on 2026-06-21 the running `symphony-host`, `homelab-temporal-patrol-worker`, `podium-web`, and `podium-api` all predated their respective feature commits (started the evening of 2026-06-20). Each required a restart to pick up the code. The schedule-UI "flicker back to No" bug was `podium-api` (started 22:44Z) predating the #96 `POST /schedule` route (committed 02:47Z) → the POST 404'd → optimistic update rolled back. — Evidence: `systemctl show … ExecMainStartTimestamp`; `/openapi.json` lacked then listed `/api/issues/{issue_id}/schedule` after restart.
- Deploy-verify of the block→schedule path on the 2026-06-21 00:00–06:00 PT window (07:00–13:00 UTC) did NOT cleanly verify. No issue was ever held (`scheduled_for` null on every row, read-only DB query). The path IS live and an agent attempted to self-schedule: issue 76 (homelab, gpt-5.5) emitted `Symphony-Schedule: not_before=2026-06-21T07:00:00+00:00 reason="aidev Docker image updates need the maintenance window…"` — a **computed ISO timestamp (= window start) instead of symbolic `next_window`**. By run completion (07:01) that instant was in the past, so the #95 handler rejected it ("Cannot schedule into the past"), classified `agent-scheduled-malformed`, and transitioned issue 76 to `blocked`. — Evidence: `journalctl -u symphony-host` (07:00–07:35Z: `state_transitioned issue_id=76 state=blocked`, `dispatch_completed reason=agent-scheduled-malformed issue_id=76`); read-only `podium.db` issue 76 `comments_md` tail. Had it emitted `next_window`, the scheduler would have resolved it to the current window start and it would be due/applied (cf. C-0292).
- Suspected root cause (NOT verified): issue 76 was a resumed session from `2026-06-20T21:02Z` (`scheduler resume_selected … session_file=…2026-06-20T21-02-16…`), which predates the policy/INFRA_PREAMBLE deploy (04:46Z). The `next_window` guidance likely was not re-injected into the resumed session.

## Decisions

- Schedule UI is Yes/No, not a 3-mode control; the Custom date-time path is hidden from the UI (API still accepts explicit `not_before`). — James, this session.
- Held (scheduled) issues should be commentable without re-dispatch, via the append-only `/comment` endpoint. — James, this session.
- Do NOT do #098 item 6 (re-open the blocked backlog 63/66/71/74/75/76 to `todo`) until a clean schedule-and-apply is observed — re-opening now would re-block on the same malformed-marker gap. — agent recommendation, accepted by deferral.

## Evidence

- homelab `f76f7ab` — #098 implementation + tests (732 passed).
- symphony `002b25f`/`9c21520`/`4047704`/`7e4a319` — schedule-UI refinement + e2e.
- `journalctl -u symphony-host` window logs — schedule-marker dispatch outcomes (76 malformed→blocked; 71/82 marker-review; 81 marker-blocked).
- read-only `podium.db` — no held issues; issue 76 comments_md shows the rejected marker.
- `systemctl show … ExecMainStartTimestamp` — stale-deploy timeline.

## Exclusions

- No secrets/credentials captured.
- Unrelated working-tree drift (homelab `data/zeroday/*`) intentionally not committed/captured.
- Full transcript not archived.

## Open Questions And Follow-Ups

- Confirm whether INFRA_PREAMBLE / the `next_window` instruction is injected into **resumed** agent sessions, not just fresh dispatches. This is the suspected leak behind issue 76.
- Re-run the deploy-verify with a fresh (non-resumed) medium-risk dispatch before flipping homelab to hands-off and before #098 item 6.
- #098 item 6 (re-open blocked backlog) remains pending, gated on a clean verify.
