---
name: symphony-project-scaffold
description: Deprecated Plane-only scaffold alias kept for rollback and legacy v2 hedge. New Podium bindings use symphony-binding-scaffold.
---

# Symphony Project Scaffold

Deprecated Plane-only path. Plane is dormant during the Podium cutover.

Use `symphony-binding-scaffold` for new Podium bindings. Keep this skill only for rollback, historical recovery, or a deliberate legacy Plane path.

## Safety rules

- Do not use for new Podium onboarding.
- Legacy Plane mutation still requires typed-slug confirmation.
- Prefer `symphony-plane-recover` for Plane retirement cleanup.
